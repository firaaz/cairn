id: compression/learnings-capture
name: compression/learnings-capture
status: failed
current_phase: 4
phase4_invocation: 3
failure-reason: "Phase 4 raise-issue, B15 redispatch cap exceeded. Three closes-when criteria unmet: agent_timeout not wired in dispatch.py; cost/token threshold breach not wired in core.py; phase-4-integrator.md missing Learnings observed section. Root cause (from debug logs): Phase 3 cluster fan-out into orchestrator-events + phase4-prompt-amendment workers; the prompt-amendment worker had no associated RED test (intent explicitly excluded prompt-content tests as brittle), so it observed GREEN test status and reported OK without ever editing .claude/agents/phase-4-integrator.md. Structural failure mode: clusters without RED tests lack a progress signal — agents conclude done from test status alone. Not a model-capability problem; bumping Sonnet medium to high or Opus low would not have fixed this."
brief: "Restore the per-slice learning-capture surface that the compressed protocol structurally lost. Add two layered surfaces: (1) Phase-4 integrator emits optional ## Learnings observed section in sweep-notes.md; (2) orchestrator emits mechanical events to .claude/current-slice/integration/orchestrator-events.jsonl (phase_redispatch, redispatch_cap_exceeded, cost_threshold_breach, token_threshold_breach, agent_timeout). Both ride existing slice-artifact-preservation pipeline. stdlib-only. adrs-referenced: [slice-artifact-preservation, phase-lock-and-role-declaration]. Feature: compression. Predecessor: slice-artifact-preservation (closed). Successor: substrate Slice 2 (lever-Y-mcp-substrate) opens after this closes."
raise_issue_reason: "Three closes-when criteria unmet after two Phase 3 redispatches with zero source-file changes: (1) agent_timeout not wired in dispatch.py, (2) cost/token threshold breach not wired in core.py, (3) phase-4-integrator.md missing Learnings observed section."
