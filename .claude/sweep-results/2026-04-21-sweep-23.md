# Integration Sweep #23 — post-`housekeeping/inv004-rebaseline-cc-2.1.116`

**Date:** 2026-04-21
**Branch:** `feature/compression`
**Tip:** `9453569`
**Previous sweep:** #22 (`2026-04-21-sweep-22.md`) — post-`compression/slice-3-observability-and-close-slice`, FAIL (12 inherited reds + 1 F841 + 2 E741, all classified).
**Window:** 1 slice closed since #22 — `housekeeping/inv004-rebaseline-cc-2.1.116` (close `fe5662c`), plus 1 mid-slice substrate hotfix (`5319c53` YAML escape), handoff (`9453569`).
**Verdict:** **FAIL** (integration gate exits 1). All failures inherited from #22, **minus** INV-004 (resolved by this window's rebaseline slice). No novel cross-slice regressions.

---

## Cross-slice failure modes (pre-check enumeration)

| Mode | Hypothesis | Outcome |
|---|---|---|
| INV-004 prose↔code drift | 30k→40k ceiling in test but ARCHITECTURE.md still cites 30k | **Clean** — `validate_architecture.py` Check D PASS; INV-004 assertion greps the new ceiling successfully |
| Rebaseline arithmetic error | Headroom convention (~36%) violated or BUDGET_HARD off-by-one | **Clean** — `test_inv004_turn1_token_budget` GREEN at 40k ceiling; observed 30170-30351 tokens well under new ceiling |
| Snapshot envelope creep | Rebaseline slice edits leaked beyond scoped 2 files | **Clean** — `snapshot_diff --diff` reports exactly `docs/ARCHITECTURE.md` + `tests/unit/test_context_budget.py`, matching the slice's declared scope |
| Mid-slice substrate hotfix contamination | `5319c53` YAML escape fix bled into rebaseline scope or altered unrelated behavior | **Clean** — `5319c53` touches `commands/claude-code/start-slice.md` YAML only; narrowly scoped; pre-existing substrate bug in the queued bug-fix scope |
| Inherited-red count drift | Reds count changed non-monotonically (i.e., new reds introduced) | **Clean** — 10 pytest reds (↓1 from #22's 11: INV-004 now GREEN); F841 and both E741s still present |
| Invariant interactions | INV-004 ceiling bump interacts with INV-002 (session-start handoff) or context-discipline budget assertions | **Clean** — handoff token-budget test GREEN; no INV-002 regression |
| Provisional-ADR drift | Provisional ADRs treated as firm | **Clean** — no new ADRs in window |
| Hidden workarounds in commits | `hack|workaround|skip|xfail|tmp|todo|fixme` in commit subjects | **Clean** — 1 `fix(slice):` commit (`5319c53`) is a legitimate pre-existing bug hotfix, subject already surfaced in bug-fix slice scope memory |

---

## Gate results

| Gate | Command | Exit | Detail |
|---|---|---|---|
| Invariant check | `integration_gate.py` Step 3 | 0 | **PASS** — 8 firm invariants validated |
| Ruff | `integration_gate.py` Step 4a | 1 | **FAIL** — 3 errors, all inherited: 2× E741 `l` in `tests/unit/test_cross_slice_isolation.py:72-73`; 1× F841 unused `result` in `tests/unit/test_post_timeout_reconcile.py:101` |
| pytest (full) | `uv run pytest --tb=no -q` | 1 | **FAIL** — 10 failed, 650 passed, 3 skipped. INV-004 now GREEN (was RED in #22); other 10 reds unchanged |
| integration_gate overall | `integration_gate.py` | 1 | FAIL-fast at Step 4a; all failures pre-classified |
| snapshot_diff | `snapshot_diff.py --diff` | 1 → 0 | 2 paths out-of-envelope (classified §3); `--snapshot` applied; re-run exits 0 |

Overall: **FAIL** (integration gate non-green; delta from #22 is −1 resolved red, no new issues).

---

## Findings

### 1. INV-004 re-baseline landed successfully — **close the handoff item**

The only charter of `housekeeping/inv004-rebaseline-cc-2.1.116` was to raise the INV-004 hard budget from 30k to 40k and update ARCHITECTURE.md prose to cite the new ceiling + CC 2.1.116 measurement. Both delivered:

- `tests/unit/test_context_budget.py` BUDGET_HARD = 40000 (was 30000)
- `docs/ARCHITECTURE.md` INV-004 body cites 40k ceiling + CC 2.1.116 measurement context
- `test_inv004_turn1_token_budget` GREEN (observed 30170-30351 tokens, ~24% headroom under new ceiling — consistent with prior re-baseline's 22k→30k +36% pattern, directionally aligned)

**Class:** resolved.
**Action:** remove INV-004 re-baseline references from next handoff's Blocked/Pending. Don't re-queue under `housekeeping/post-inv008-tech-debt`.

### 2. Inherited red roster — **12 items carry forward (was 14 at #22)**

Full remaining roster for the bundled housekeeping-bugfix slice:

**pytest (10 reds):**
- learning.md cap → `test_context_discipline_protocol::test_v5_learning_staging_ground_exists_and_is_minimal`
- relpath bypass (3) → `test_hook_relpath_bypass::{test_edit_body_bare_relative_flat_slug_blocked, test_edit_body_bare_relative_legacy_blocked, test_edit_body_slice_system_flat_slug_blocked}`
- hook tolerance (3) → `test_hook_tolerance::TestV3FrontmatterEditBothForms::{test_edit_body_flat_slug_blocked, test_edit_body_flat_slug_emits_message, test_edit_body_legacy_blocked}`
- handoff-tracked → `test_housekeeping_post_slice_a_tidy::test_item_b_handoff_not_tracked`
- INV-008 assertion counts (2) → `test_invariant_assertions::TestSlice011AssertionCoverage::{test_no_extra_assertion_blocks, test_invariant_count_unchanged}`

**ruff (3 errors):**
- 2× E741 `l` in `tests/unit/test_cross_slice_isolation.py:72-73`
- 1× F841 unused `result` in `tests/unit/test_post_timeout_reconcile.py:101`

**Delta from #22:** INV-004 resolved (`test_context_budget::test_inv004_turn1_token_budget`). No new reds introduced by the rebaseline slice.

**Class:** pre-existing, all 12. Queued for `housekeeping/post-inv008-tech-debt` in handoff.

### 3. Snapshot drift: 2 paths out-of-envelope — **expected rebaseline envelope**

Both paths are the exact files declared in `slice.yaml`'s scope:
- `docs/ARCHITECTURE.md` — INV-004 body prose update
- `tests/unit/test_context_budget.py` — BUDGET_HARD constant raise

Class: `slice-caused (C)`, expected envelope. Baseline refreshed at `9453569`; re-run of `snapshot_diff --diff` exits 0.

### 4. Mid-slice substrate hotfix `5319c53` — **valid inline fix, reconcile in bug-fix slice**

During Phase-3 dispatch, the skeptic-agent YAML escape bug fired (one of the 5 substrate bugs staged for the bug-fix slice per memory `orchestrator_bug_fix_slice_scope.md`). The slice recovered with an inline substrate hotfix at `5319c53` (`fix(slice): coupling-clusters YAML escape — unblock phase 3 dispatch`).

**Observation:** this is a legitimate pipeline-substrate commit under rule 9 (failures → new slices) **because** the surrounding work was a housekeeping slice and the bug was pre-existing — not a retroactive edit to a completed slice. The inline fix unblocked forward progress without scope creep.

**Reconciliation needed for the bundled housekeeping-bugfix slice:** confirm whether `5319c53` fully resolved the "phase-2-skeptic YAML-escape" item on the 5-bug list, or whether it was a narrower one-off patch (e.g., `coupling-clusters` specifically) that left the broader YAML-escape class unresolved. Phase-1 writer of the bundled slice should cite `5319c53` when scoping this item.

**Class:** non-blocking observation; inform bug-fix slice scoping.

### 5. Identifier drift: slice.yaml uses `inv004-rebaseline-cc-2-1-116` (hyphens), commit subjects/branches use `inv004-rebaseline-cc-2.1.116` (dots) — **confirmed substrate bug**

The rebaseline slice's `slice.yaml` id/name is hyphenated (`housekeeping/inv004-rebaseline-cc-2-1-116`) while commit subjects, brief prose, and proposed slice-id inside the brief field all use dots (`housekeeping/inv004-rebaseline-cc-2.1.116`). This matches the "slug dot/hyphen drift" bug on the 5-bug list.

**Class:** pre-existing substrate bug, confirmed by real-world slice run.
**Action:** already queued in the bundled housekeeping-bugfix slice.

---

## Staleness summary (handoff Blocked/Pending audit)

| Handoff item | Status | Action |
|---|---|---|
| INV-004 CC 2.1.116 re-baseline | **RESOLVED** by `fe5662c` | Remove from next handoff's Blocked/Pending; `housekeeping/post-inv008-tech-debt` no longer needs to include the re-baseline |
| 10 pytest reds + 2 E741 + 1 F841 → `housekeeping/post-inv008-tech-debt` | **Still valid** | Confirmed by this sweep (note: count is 10 reds, not 11 — INV-004 is now resolved) |
| `CAIRN_HEARTBEAT_INTERVAL` + `CAIRN_HEARTBEAT_STALE` docs at `operational-reference.md:339-344` | **Still valid** | Not touched by rebaseline slice |
| 5 substrate bug-fix slice items (memory `orchestrator_bug_fix_slice_scope.md`) | **Still valid** | One item (coupling-clusters YAML escape) received an inline hotfix at `5319c53` — reconciliation required in the bundled slice's Phase 1 scoping |
| Triager misroute on superseded tests → prompt iteration | **Still valid** | Not touched by rebaseline slice |
| Path C + multi-instance hardening → deferred | **Still valid, deferred** | No action this sweep |

**Handoff count discrepancy note:** handoff.md:16 says "Substrate bug-fix slice (after tech-debt) — 4 items" but memory `orchestrator_bug_fix_slice_scope.md` lists 5 items (current_phase persistence + YAML-escape + slug drift + measurement churn + close_slice subject). The bundled slice's Phase 1 should use the memory's 5-item list, not the handoff's 4.

---

## Follow-up slice candidates (for operator triage)

**Operator has already chosen bundling** — single slice combining #1 and #2 below:

1. **`housekeeping/post-inv008-tech-debt`** — 10 pytest reds + 2 E741 + 1 F841 + heartbeat env docs.
2. **Substrate bug-fix slice** — 5 items per memory `orchestrator_bug_fix_slice_scope.md`:
   - `current_phase` not persisted across sessions
   - `phase-2-skeptic` YAML-escape (partially fixed inline at `5319c53` — confirm class vs. narrow patch)
   - Slug dot/hyphen drift (confirmed live this sweep — §5)
   - Measurement churn
   - `close_slice` subject fails `verify_handoff` check (c)

**Recommended bundled slice id:** `housekeeping/post-inv008-and-substrate-bugs` or similar. Phase-0 brainstorming will finalize naming and decide whether to split on size grounds.

3. **Triager prompt iteration** (no slice id yet, distinct) — before the next firm-contract slice; memory `triager_misroute_on_superseded_tests.md` has context.

No new firm ADRs required. No novel cross-slice regressions.

---

## Next action

Bump `sweep.yaml` → `last-sweep-at-slice-id: housekeeping/inv004-rebaseline-cc-2-1-116`. Commit sweep report + yaml bump together. Then `/start-slice` for the bundled housekeeping-bugfix slice per operator decision.
