# Integration Sweep #15 — post-SLICE-020 (configurable-pytest-timeout)

**Date:** 2026-04-17
**Branch:** `feature/identifier-scheme`
**Tip:** `7258f77`
**Previous sweep:** #14 (`2026-04-16-sweep-14.md`) — coord-level post-merge (manual Axis-B dogfood), PASS with 1 finding
**Verdict:** **PASS with 1 carried-over finding** — gates are clean except the SLICE-018 `test_d3_bypass_log_format` suite, which is a pre-existing failure documented in SLICE-018 and SLICE-020 D3 bypass log (follow-up slice owed; not a regression).

---

## Cross-slice failure modes (pre-check enumeration)

| Mode | Hypothesis | Outcome |
|---|---|---|
| Env-var contract drift | SLICE-020 introduced `CAIRN_PYTEST_TIMEOUT` / `CAIRN_RUFF_TIMEOUT`; defaults may diverge from prior hardcoded values | Clean — ruff PASS, `_positive_int_env` in place at `scripts/integration_gate.py:82-91`; defaults preserved |
| Template edits vs. invariant anchors | SLICE-019 edited `CLAUDE.md`, `commands/claude-code/*.full.md`, `docs/operational-reference.md`; INV-002/003/004/007 grep anchors target operational-reference and handoff.full.md | Clean — INV check PASS (7/7) |
| D3 bypass log format regression | Known 4 SLICE-018 test failures; SLICE-019/020 might introduce new breakage | Confirmed pre-existing (4/4 fail, identical to SLICE-018/SLICE-020 bypass-log note) — **not regressed** |
| Context-budget drift | INV-004 machine-check; new env-var constants / doc wording could bump tokens | Clean — INV check PASS |
| Out-of-envelope edits (snapshot vs. envelope) | `scripts/integration_gate.py` + new test are only delta since f1f8cf6 baseline | Expected — matches SLICE-020 envelope exactly; baseline refreshed |
| Identifier-scheme migration (INV-005 tolerance) | Both legacy `NNN-slug.md` and flat-slug ADR filenames must parse | Clean — validator still handles both per v1-defense-d3 validator widening |

---

## Gate Results

| Gate | Command | Exit | Detail |
|---|---|---|---|
| Integration gate — Step 3 (invariants, Check D) | `integration_gate.py` | 0 | PASS — 7 invariants |
| Integration gate — Step 4a (ruff) | `integration_gate.py` | 0 | PASS |
| Integration gate — Step 4b (pytest) | `integration_gate.py` | **1** | FAIL — 4 pre-existing failures in `test_d3_bypass_log_format.py` (carried from SLICE-018) |
| Snapshot diff (pre-refresh) | `snapshot_diff.py --diff` | 1 | 2 changes: `scripts/integration_gate.py` (SLICE-020 envelope), `tests/unit/test_integration_gate_timeout.py` (new, SLICE-020 envelope) |
| Snapshot refresh | `snapshot_diff.py --snapshot` | 0 | Baseline regenerated |

**Overall:** integration_gate exit 1 is entirely driven by the carried-over SLICE-018 failures; no new breakage.

---

## Findings

### Finding #1 — `test_d3_bypass_log_format` brittleness (carried from Sweep #14 Finding #1)

**Test file:** `tests/unit/test_d3_bypass_log_format.py`
**Failures (4):**
- `test_log_has_exactly_four_lines` — asserts `len(_lines()) == 4`, actual: **7**
- `test_every_line_matches_classified_regex` — SLICE-018 line 5 has parenthetical `(A):` prefix that does not match `^SLICE-\d+ \d{4}-\d{2}-\d{2} (slice-caused|pre-existing|false-positive): .+$`
- `test_all_lines_classified_pre_existing` — same regex miss
- `test_line_order_is_chronological` — expects 4-id list, got 7

**Root cause:** Sweep #14 identified this exact brittleness (`len == 4` hardcode + strict regex). Since #14, three new entries were appended (SLICE-018 two more + SLICE-020), bringing the log to 7 lines. Regex variant for `(A)` / `(C)` class-qualifier prefixes was never accommodated.

**Scope note:** Per sweep rule 9, no retroactive edits. A follow-up slice is still owed.

**Recommendation:** Fix slice should:
1. Replace `== 4` with a format-per-line check.
2. Widen regex to accept optional `(A|B|C|D)` class-qualifier after category.

**Priority:** still high — this gate is noisy across every subsequent sweep.

### Finding #2 — handoff Blocked/Pending staleness

- `v1-defense-d2: SLICE-010/011 queued` (line in handoff.md § Features) — **STALE**. Both slices completed on `dev` at `7e4e87f` (SLICE-010) and `6d5f9f0` (SLICE-011); git history shows Check D (invariant-check blocks, machine-checkable assertions) landed via these. Feature is effectively complete. (Sweep #14 carried the same staleness forward; flagging now to correct.)
- `v1-defense-d3: SLICE-018 landed; substrate queued` — partially accurate. D3 substrate (integration_gate.py + snapshot_diff.py) already landed via SLICE-012/014/016/017/018. Remaining substrate = d3-bypass-classification Decision 2 `exempt:` syntax + `snapshot_diff.py` classified-format parser (both listed in sweep #14 next-slice recommendations, still pending).

### Finding #3 — known carried-over non-staleness (all valid)

- `docs/plans/measurements/2026-04-12-slice-003.txt` uncommitted (3-line delta) — session-start hook auto-drift per prior operator directive. No action.
- 5 reviewer suggestions → lessons-log (CAIRN_DEBUG, whitespace comment, shared `_env.py`, float-rejection comment, integration-style timeout test) — still pending.
- Feature 1 rename queue (`adr-rename-sweep → slice-and-feature-rename → doc-sweep`) — git log shows only the handoff queue-line at `24b7dc5`; no rename slices started yet.

---

## Multi-Module / Workaround Review (git log -20)

- **SLICE-020** (4 commits + 3 handoff-only) — envelope was `scripts/integration_gate.py` + new `tests/unit/test_integration_gate_timeout.py`. Both captured in snapshot diff. Clean.
- **Three handoff-only commits in quick succession** (`24b7dc5` → `d9f71be` → `b9f5178`) bracketed the SLICE-019 close and SLICE-020 redirect — normal handoff-rewrite churn as priorities shifted (sweep-due → integration-gate timeout fix → ready for integration-sweep). Not a concern, but an observation: three rewrites in <24h is on the high side of the handoff-edit rate.
- **SLICE-019** (`c0bbd92`) legitimately multi-module (`CLAUDE.md`, 4 `commands/claude-code/*.full.md`, `docs/operational-reference.md`, implementation notes) — this was the identifier-scheme §D7 Phase 1 substrate; envelope-expansions.log had entry added.
- **No `ADR_*_BYPASS=1` invocations** — no hook bypasses in window.
- **No provisional-as-firm ADR citations** — ADR-003, ADR-007, d3-bypass-classification remain provisional; SLICE-020 referenced none of them as firm.

---

## Handoff Staleness Check

Cross-referenced `.claude/handoff.md` Blocked/Pending + Features against git history:

| Entry | Status |
|---|---|
| Sweep due → `/integration-sweep` first | **IN PROGRESS** (this sweep) — will clear on commit |
| SLICE-018 — 4 pre-existing regex failures | **STILL PENDING** — confirmed 4/4 failing (Finding #1) |
| Feature 1 rename queue (adr-rename-sweep → slice-and-feature-rename → doc-sweep) | **STILL PENDING** — no rename commits in log |
| `docs/plans/measurements/2026-04-12-slice-003.txt` uncommitted | **STILL PENDING** — operator directive to ignore |
| 5 reviewer suggestions → lessons-log | **STILL PENDING** — no lessons-log entries |
| `integration-gate: complete` | Accurate |
| `identifier-scheme: Phase 1 complete; rename sweeps queued` | Accurate |
| `housekeeping: complete (SLICE-017/018)` | Accurate |
| `v1-defense-d2: SLICE-010/011 queued` | **STALE** (Finding #2) — both on dev |
| `v1-defense-d3: SLICE-018 landed; substrate queued` | **PARTIAL STALE** (Finding #2) — substrate partially landed; remaining work is narrower than "substrate" implies |

---

## Next-Slice Recommendations (priority order, all tiny)

1. **`test_d3_bypass_log_format` resilience fix** (Finding #1) — HIGH priority. Noisy across every sweep since #14.
2. **Handoff staleness correction** (Finding #2) — coincident with next handoff edit; not a slice.
3. **Feature 1 rename trio** (`adr-rename-sweep → slice-and-feature-rename → doc-sweep`) — as already queued per handoff.
4. **`snapshot_diff.py` classified-format parser** — carried from sweep #14 next-slice list.
5. **d3-bypass-classification Decision 2 `exempt:` syntax** — carried from sweep #14.
6. **`start-slice.full.md:224` rolling-window text fix** — carried from sweep #14.
7. **5 SLICE-020 reviewer suggestions → lessons-log** — process carry-over.

---

## Sweep Output

- Report: this file
- `.claude/sweep.yaml` → `last-sweep-at-slice: 20` (bumped from 18)
- Snapshot baseline: refreshed at sweep time
