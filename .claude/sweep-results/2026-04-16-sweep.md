# Integration Sweep #11 — post-SLICE-016 boundary

**Date:** 2026-04-16
**Slices since last sweep (#10):** SLICE-016 (identifier-scheme/hook-tolerance, complete)
**Previous sweep:** #10 at SLICE-015 (`.claude/sweep-results/2026-04-15-sweep.md`, commit `5d7bc1a`)
**Verdict:** FAIL — INV-004 turn-1 budget regression on CC 2.1.110 persists; D3 bypass rolling window tripped (3/10); two carry-over drifts still uncommitted.

## Failure Modes Enumerated (Pre-Check)

| Mode | Hypothesis going in |
|---|---|
| Import conflicts | None expected — SLICE-016 touched only `checks/reversibility-guard.sh` globs + tests |
| Schema drift | None expected — no ADR/feature/slice schema touched |
| Tool contract breaks | Widened globs could regress legacy-numeric-prefix ADR protection; hook-tolerance tests (25) must cover both shapes |
| Config conflicts | None expected — no `pyproject.toml` / `uv.lock` change by SLICE-016 |
| Boundary violations | Low — envelope small; watch for stray commits outside slice |
| Invariant interactions | INV-004 known-red on CC 2.1.110 (handoff entry); INV-005 validator-proxy gap remains |
| D3 bypass rolling window | SLICE-012 + SLICE-014 already logged; any new bypass would hit 3/10 → design review |

## Gate Results

| Check | Result | Notes |
|---|---|---|
| `integration_gate.py` Step 3 (invariants) | PASS | 7 invariants file-checked |
| `integration_gate.py` Step 4a (ruff) | PASS | clean |
| `integration_gate.py` Step 4b (pytest) | **FAIL** | `test_inv004_turn1_token_budget` — 28511 tokens > 22000 budget. CC: 2.1.110. Delta vs D1 baseline (27314): +1197. Known regression, ticketed as housekeeping. |
| `snapshot_diff.py --diff` | PASS | exit 0, no out-of-envelope changes |
| Baseline updated | Yes | `snapshot_diff.py --snapshot` re-ran after report |

## Out-of-Envelope Artifacts

- **`docs/plans/measurements/2026-04-12-slice-003.txt`** — uncommitted diff (CC 2.1.107 → 2.1.110 measurement refresh reflecting the INV-004 regression). **5th sweep carry-over** (prior: #5, #7, #8, #9, now #11). Not touched by SLICE-016.
- **`uv.lock`** — uncommitted drift expanding the virtual-package metadata and loosening `requires-python` to `>=3.11`. Related to SLICE-005/SLICE-007 envelope red flagged in handoff. Not touched by SLICE-016.
- No stray worktree drift this sweep — `.claude/worktrees/fervent-mcnulty/` was not re-flagged; verify removal in next sweep.

## Cross-Slice Failure Mode Check

| Mode | Finding |
|---|---|
| Import conflicts | None. No import edits in SLICE-016. |
| Schema drift | None. ADR/feature/slice schemas untouched. |
| Tool contract breaks | None. `reversibility-guard.sh` globs widened additively; 25/25 hook-tolerance tests GREEN before close; legacy `NNN-slug` and flat-slug ADRs both enforced as append-only. |
| Config conflicts | None in SLICE-016 envelope. `uv.lock` drift is pre-existing debt. |
| Boundary violations | None this sweep — SLICE-016 commits stayed in envelope. |
| Invariant interactions | INV-004 regression confirmed on CC 2.1.110 (see below); INV-005 validator-proxy gap unchanged (still cites ADR-006 as operational proxy for `identifier-scheme`). |
| Test bypass staleness | **THRESHOLD FIRED** — D3 bypass log at 3/10 (SLICE-012, SLICE-014, SLICE-016). ADR-003 D1 review criterion invokes at 3-in-rolling-10; design review is now mandatory, not optional. |

## Invariant Evidence

| Invariant | Evidence | Verdict |
|---|---|---|
| INV-001 | Every commit since #10 is pipeline-class (`slice:`, `handoff:`) — no direct non-slice edits | PASS |
| INV-002 | `.claude/handoff.md` = 30 lines, ~245 tokens (estimated); fixed sections intact | PASS |
| INV-003 | SLICE-016 executed 4 phases in order per commit graph (`5ec141f` phase 1 → `49304a3` phase 2 → `d01ab72` phase 3 → `6b0f9df` complete) | PASS |
| INV-004 | `tests/unit/test_context_budget.py::test_inv004_turn1_token_budget` FAIL: 28511 > 22000 on CC 2.1.110 (baseline 27314 on 2.1.107) | **FAIL** |
| INV-005 | `docs/adr/identifier-scheme.md` exists (file-check); `reversibility-guard.sh` now tolerates both shapes per hook-tolerance tests | PASS |
| INV-006 | `.claude/features/*.yaml` matches; `identifier-scheme.yaml` SLICE-016 entry now `after: identifier-scheme/scheme-adr` closed | PASS |
| INV-007 | `commands/claude-code/handoff.full.md` still references `.claude/features/` at Tier 2 | PASS |

**INV-004 note.** The hard failure is well-characterized and not a regression introduced by SLICE-016. Root cause is CC binary drift (2.1.107 → 2.1.110) increasing system-prompt overhead; slice envelopes are not loaded at session start, so this is visible in every fresh session regardless of slice state. Housekeeping slice must choose: re-baseline hard budget upward (22000 → ~29500) with named justification, or diagnose whether CC added prompt mass that cairn can avoid by pruning `.claude` tier-1 content. Cannot stay in this limbo — the integration gate will fail every sweep until resolved.

## Handoff Staleness Check

| handoff.md "Blocked / Pending" entry | Status after this sweep |
|---|---|
| D3 bypass log at 3/10 → design review recommended | **Still pending — and now mandatory, not recommended.** Threshold fired at SLICE-016 close. |
| INV-004 turn-1 budget fails on CC 2.1.110 | Still pending. Integration gate now red until re-baseline or cairn-side prune. |
| `uv.lock` envelope red (SLICE-005/-007) → housekeeping | Still pending. `uv.lock` diff persists (4th+ carry-over). |
| `validate_architecture.py` flat-slug recognition → follow-on | Still pending. INV-005 still proxies via ADR-006. |
| Relative-path bypass in `reversibility-guard.sh` | Still pending. Not addressed by SLICE-016 (explicit non-goal in intent). |

No falsely-pending entries. Nothing quietly resolved.

## Action Items

1. **D3 design review is now required.** `/decision` on the D3 bypass pattern. All three bypasses (SLICE-012 ruff debt, SLICE-014 parallel-session fleet-coordinator doc, SLICE-016 INV-004 CC drift) are **not** D3 failures — they are known pre-existing drifts outside the slice's envelope. Question for the decision: does the 3-in-10 threshold correctly discriminate between "D3 genuinely failing" and "D3 correctly flagging pre-existing debt we haven't cleared"? Consider: per-reason classification, separate counters, or named exemption for pre-logged drift carried across slices.
2. **Housekeeping slice — INV-004 re-baseline.** Pick one: (a) raise `BUDGET_HARD` in `test_context_budget.py` with CC-version-stamped justification; (b) diagnose cairn-side tier-1 prune opportunity (inspect what cumulates in the 28511 figure). Currently integration gate cannot pass.
3. **Housekeeping slice — drift cleanup.** Commit or revert `docs/plans/measurements/2026-04-12-slice-003.txt` and `uv.lock`. The measurements file is co-dependent with item #2 — the numbers only land after re-baseline. `uv.lock` is older and independent.
4. **Follow-on identifier-scheme slices.** Two queued: (a) widen `validate_architecture.py` to recognize non-numeric ADR IDs in `**INV-NNN**` lines so INV-005 can cite `identifier-scheme` directly; (b) close the relative-path bypass in `reversibility-guard.sh`. Neither is blocking; sequence after housekeeping.

## Not a Regression

- Snapshot diff clean (exit 0) — no unexpected file-shape drift.
- Invariant file-existence checks all pass.
- SLICE-016's own target scope (widen hook globs) is behaving: 25/25 hook-tolerance tests green per slice-close, `test_v2_frontmatter_valid` not re-broken.

The gate failure is pre-existing CC-drift debt, not SLICE-016 damage. But the gate cannot distinguish "known regression" from "new breakage" — housekeeping must land before the next slice's Phase 4 Auditor can trust a green integration-sweep signal.
