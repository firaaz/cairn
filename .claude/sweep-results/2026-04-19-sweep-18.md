# Integration Sweep #18 — post-`compression/doc-cleanup-tail` (single-slice window)

**Date:** 2026-04-19
**Branch:** `feature/compression`
**Tip:** `87ffc87`
**Previous sweep:** #17 (`2026-04-18-sweep-17.md`) — post-`identifier-scheme/{slice-and-feature-rename,all-seven,doc-sweep}` three-slice window, FAIL carry-over on `test_d3_bypass_log_format` regex.
**Verdict:** **PASS** — integration gate all green under `env -u ADR_EDITORIAL_FIX`; validator Check D green on 7 firm invariants; 11 pre-slice drift paths classified and snapshot baseline refreshed. Sweep #17's carry-over failures are resolved — the regex at `tests/unit/test_d3_bypass_log_format.py:25` now accepts both `SLICE-\d+` and hierarchical `<feature>/<slice>` shapes (landed in `68259fd`).

---

## Cross-slice failure modes (pre-check enumeration)

| Mode | Hypothesis | Outcome |
|---|---|---|
| Phase-artifact residue from reshaped Slice A | `compression/doc-cleanup-tail` reshaped mid-kickoff to absorb identifier-scheme debt; could leave inconsistent slice-artifact state | Clean — slice committed complete at `87ffc87`; no open `.claude/current-slice/` state; handoff shows `slice: none phase: complete` |
| INV-005 file-exists anchor broken by any residual rename | `identifier-scheme.md` still the canonical anchor post-merge | Clean — validator passes; 11 ADRs checked |
| Hook tolerance widened-glob admitting unintended paths | `scope-guard.sh` / `reversibility-guard.sh` canonicalization via `PROJECT_ROOT` strip + `.slice-system/` strip | Clean — hook unit suite 60 passed under `env -u ADR_EDITORIAL_FIX`; one false failure surfaced from leaked shell env (see Findings §1) |
| Snapshot baseline stale from prior-slice work | Handoff blocked-pending item; carry-over from sweep #17 note "baseline refresh queued" | Resolved — 11 drift paths all map to pre-slice work; `--snapshot` executed; baseline current |
| `test_d3_bypass_log_format` hierarchical-ID regression | Sweep #17 flagged root-fix `v1-defense-d3/bypass-log-test-resilience` as stale pointer | Resolved — regex already accepts both shapes (landed in `68259fd`); queued follow-up in handoff is STALE |
| Provisional ADRs prose-promoted to firm | `cliff-failure-mode-and-v1-defenses`, `parallelism-v1`, `d3-bypass-classification` remain provisional | Clean — no `(firm)` promotions in ADR frontmatter |

---

## Gate Results

| Gate | Command | Exit | Detail |
|---|---|---|---|
| Integration gate — Step 3 | `integration_gate.py` | 0 | PASS — invariant Check D (7 firm invariants) |
| Integration gate — Step 4a | `integration_gate.py` | 0 | PASS — ruff |
| Integration gate — Step 4b | `integration_gate.py` | 0 | PASS — full pytest (460 passed, 1 skipped) under `env -u ADR_EDITORIAL_FIX` |
| Snapshot diff | `snapshot_diff.py --diff` | 1 | 11 pre-slice drift paths (expected; baseline refresh owed) |
| Snapshot refresh | `snapshot_diff.py --snapshot` | 0 | Baseline rewritten at `87ffc87` |
| Validator | `validate_architecture.py` | 0 | 7 invariants verified, 11 ADRs checked |

Overall: **PASS.**

---

## Findings

### 1. Shell-env leak of `ADR_EDITORIAL_FIX=1` produces false test failure

Invoking `uv run python scripts/integration_gate.py` without stripping the inherited shell env causes `tests/unit/test_hook_relpath_bypass.py::TestV1BareRelativeFlatSlugBlocked::test_edit_body_bare_relative_flat_slug_blocked` to fail. Traced via `bash -x`: the hook at `checks/reversibility-guard.sh:81-84` honors the editorial-fix escape hatch and exits 0 on ADR body-edits when `ADR_EDITORIAL_FIX=1` is inherited. This matches the handoff note "D3 Gate A clean under `env -u ADR_EDITORIAL_FIX`" — the leak is a session-hygiene artifact, not a regression. No code change warranted. Consider: persistent-env hygiene guidance in `docs/operational-reference.md` (not this sweep's scope).

### 2. Snapshot drift: 11 pre-slice paths classified

All 11 paths that triggered `snapshot_diff --diff` predate `compression/doc-cleanup-tail` phase 1 (`635f2e7`). Classification per `d3-bypass-classification`:

| Path | Source commit | Class |
|---|---|---|
| `docs/adr/d3-bypass-classification.md` | `c72a805` (Slice A phase 3) + `68259fd` (identifier-scheme) | pre-existing |
| `docs/dogfood-log.md` | `68259fd` | pre-existing |
| `docs/features/identifier-scheme.md` | `10e46ba` (closeout) | pre-existing |
| `docs/lessons.md` | `10e46ba` + `68259fd` | pre-existing |
| `docs/plans/2026-04-16-dogfood-observations.md` | `68259fd` | pre-existing |
| `docs/plans/2026-04-16-manual-parallel-dogfood.md` | `68259fd` | pre-existing |
| `docs/plans/2026-04-16-slice-018-d3-oob.md` | `68259fd` | pre-existing |
| `docs/plans/2026-04-18-feature-to-dev-merge-protocol-design.md` | `68259fd` | pre-existing |
| `docs/plans/2026-04-18-feature-to-dev-merge-protocol-plan.md` | `68259fd` | pre-existing |
| `tests/unit/test_d3_bypass_log_format.py` | `68259fd` | pre-existing |
| `tests/unit/test_phase_rethink.py` | `f1e7d54` (Slice A phase 2) + `68259fd` | pre-existing |

Root cause: sweep #17 ran on `feature/identifier-scheme` against its own baseline; merge to `feature/compression` plus one completed slice accrued 11 paths without interim baseline refresh. No new `.claude/d3-bypasses.log` entry from this sweep — the sweep itself is the resolution.

### 3. Stale handoff pending entry flagged

`.claude/handoff.md` Blocked/Pending line: `v1-defense-d3/bypass-log-hierarchical-slug canonical slice queued` — this follow-up is obsolete. The test file at `tests/unit/test_d3_bypass_log_format.py:25` already accepts hierarchical slice IDs (`r"^(?:SLICE-\d+|[a-z0-9][a-z0-9-]*/[a-z0-9][a-z0-9-]*) ..."`), landed in `68259fd`. All rolling-window bypass entries from `identifier-scheme/*` and `compression/doc-cleanup-tail` parse cleanly. Recommend: drop this line from handoff at next session-close.

### 4. `last-sweep-at-slice-id` updated

`identifier-scheme/doc-sweep` → `compression/doc-cleanup-tail`.

---

## Handoff Staleness Audit

| Item | Status |
|---|---|
| Integration sweep due; snapshot baseline refresh belongs here | RESOLVED (this sweep) |
| Compression Slice B — Part 0 ADR consolidation queued | VALID (not yet shaped) |
| `v1-defense-d3/bypass-log-hierarchical-slug` canonical slice queued | **STALE** — fix already landed in `68259fd`; drop on next handoff |
| `docs/plans/measurements/2026-04-12-slice-003.txt` regenerates on every pytest run | VALID (tracking deferred) |
| No `origin` remote configured | VALID |

---

## Next

- Next sweep after 1 more slice (interval unchanged).
- Drop stale pending entry on next handoff.
- Compression Slice B (Part 0 ADR) ready to be shaped; uses OLD serial `/start-slice` per design doc §208.
