# Integration Sweep #21 — post-`compression/slice-1-foundation` (3-slice window)

**Date:** 2026-04-20
**Branch:** `feature/compression`
**Tip:** `5a8bdcd`
**Previous sweep:** #20 (`2026-04-19-sweep-20.md`) — post-`housekeeping/post-slice-a-tidy`, PASS.
**Window:** 3 slices closed since #20 — `identifier-scheme/rename-sweep-test-robust` (`c5007c4`), plus pre-Slice-1 pipeline-substrate commits (hardening design+plan, A1 spike results), plus `compression/slice-1-foundation` (`5a8bdcd`).
**Verdict:** **PASS** — no integration regressions introduced; all tests green under clean env. Two process findings (Phase 4 artifact gap on Slice 1; handoff staleness) documented as follow-ups, not blockers.

---

## Cross-slice failure modes (pre-check enumeration)

| Mode | Hypothesis | Outcome |
|---|---|---|
| Stdlib-only drift in new Python | `slice_orchestrator.py` dispatch split + tee-pump + balanced-brace tail introduce 3rd-party imports | Clean — ruff PASS; no new deps |
| Dispatch contract split breaks orchestrator call sites | `dispatch_phase_agent` / `dispatch_triager` return shapes mis-consumed | Clean — `test_dispatch_contract_separation` (6/6), `test_orchestrator_live_stderr` (new), `test_multiline_json_tail` (new), `test_role_guard_wired` (new) all GREEN |
| `PERMISSION_MODE=bypassPermissions` leak into non-compression code paths | A1 unblock escape hatch consumed by unintended call sites | Clean — env var documented in intent, only consumed by orchestrator `dispatch_phase_agent` subprocess env; no broader harness plumbing |
| role_guard hook misfire on non-AGENT_ROLE contexts | `checks/role_guard.py` wired in settings but fails closed when `AGENT_ROLE` unset | Clean — `test_role_guard_wired` asserts no-op when unset; 565 tests including 85+ hook tests pass |
| Snapshot baseline stale across 3-slice window | Multi-slice accumulation drift masking real regressions | **Partial** — 13 out-of-envelope paths surfaced (9 expected, 4 pre-slice-substrate); classified below, baseline refreshed |
| Phase-boundary immutability gate (INV-003) violation | One of three closed slices skipped committed artifact at a phase transition | **Finding §1** — `compression/slice-1-foundation` has no phase-4 commit; sweep-notes.md artifact referenced by handoff never entered git |
| INV-002 / INV-004 token-budget interactions | Handoff / session-start regression | Clean — `handoff.md` 29 lines; `test_context_budget.py` green |
| Provisional-as-firm treatment | `compression-infrastructure-bootstrap` or `parallelism-v1` quietly relied on as firm | Clean — both still `firmness: provisional` in `docs/adr/`; no new references elevating firmness |
| `ADR_EDITORIAL_FIX` env-leak from prior sweeps persisting | Sweep #18 Finding §1 still masking real vs. spurious failures | **Finding §2 confirmed** — integration_gate under default env shows 1 failure (halt at `test_hook_relpath_bypass`); under `env -u ADR_EDITORIAL_FIX` all 565 tests pass |

---

## Gate results

| Gate | Command | Exit | Detail |
|---|---|---|---|
| Invariant check | `integration_gate.py` Step 3 | 0 | PASS — 7 firm invariants via `validate_architecture.py` Check D |
| Ruff | `integration_gate.py` Step 4a | 0 | PASS |
| pytest (default env) | `integration_gate.py` Step 4b | 1 | FAIL-fast — `test_hook_relpath_bypass::test_edit_body_bare_relative_flat_slug_blocked` returns exit 0 (expected 2) under leaked `ADR_EDITORIAL_FIX=1` |
| pytest (clean env) | `env -u ADR_EDITORIAL_FIX uv run pytest tests/` | 0 | **565 passed, 3 skipped** — all failures are env-leak artifacts |
| integration_gate (clean env) | `env -u ADR_EDITORIAL_FIX python3 scripts/integration_gate.py` | 0 | PASS — all 3 steps green |
| snapshot_diff | `snapshot_diff.py --diff` | 1 → 0 | 13 paths out-of-envelope (classified §3); `--snapshot` applied; re-run exits 0 |
| D3 bypass log rolling window | `.claude/d3-bypasses.log` tail-10 | — | 0 `false-positive` entries; 3-in-10 design-review trigger not armed |

Overall: **PASS** (integration green under clean env; findings are process/governance, not code regressions).

---

## Findings

### 1. Phase 4 artifact gap on `compression/slice-1-foundation` — **real, process finding**

The slice went Phase-3 (`f488801`, dispatch-core) → close (`5a8bdcd`, wipe+d3-bypasses) with **no Phase-4 commit**. All other slices in the 5-commit comparable window emit a `phase-4: <slice> integration — ...` or `handoff: phase 4 sweep PASS ...` commit before close:

- `identifier-scheme/rename-sweep-test-robust`: `6ee9265 handoff: phase 4 sweep PASS (artifacts pending)` → `c5007c4 complete`
- `compression/orchestrator-hardening`: `3fffd79 phase-4: auditor verdict` → `fc1de9d complete`
- `housekeeping/post-slice-a-tidy`: `880337b phase-4: integration — 8/8 GREEN` → `033c82d complete`
- `compression/infrastructure`: `141148e phase-4: integration — sweep #19 PASS` → `a09e5e8 complete`

Slice 1 close commit `5a8bdcd` touches only `.claude/current-slice/*` (deletions), `.claude/d3-bypasses.log` (2 appended entries). Git `--diff-filter=A` for `.claude/current-slice/integration/sweep-notes.md` shows no add inside Slice 1's window — the file was never committed. Yet Phase 4 demonstrably ran: the two 2026-04-20 D3 classifications in `.claude/d3-bypasses.log` are Phase-4 auditor output.

**Class:** governance — `slice-caused` (C). The artifact referenced by `handoff.md:18` ("sweep-notes bundle §Orchestrator bugs surfaced") is not in git. Under the four-phase-lock definition (ARCHITECTURE.md Boundaries §4, INV-003), the Integration→complete transition requires a committed sweep-notes.md with PASS verdict and citation evidence. This commit is absent.

**Impact:** the 3 orchestrator bugs the handoff promises are not preserved outside the working Claude session memory that produced them. If no operator re-documents them, Slice 2 loses that signal.

**Fix candidate (housekeeping micro-slice):** `housekeeping/slice-1-phase-4-retrodoc` — recover the orchestrator-bugs list (either from a reflog/stash if present, or from the `.claude/orchestrator-debug/*.log` files Slice 1 left behind, or by re-running Phase 4 as a retrospective audit) and commit as `.claude/sweep-results/2026-04-20-slice-1-retrodoc.md`. Do NOT retro-edit Slice 1 itself (rule 9).

### 2. `ADR_EDITORIAL_FIX` env-leak: handoff's "7 pre-existing failures" is stale — **real, handoff-update finding**

`handoff.md:17` claims "7 pre-existing test failures (`test_hook_relpath_bypass` + `test_hook_tolerance` + `test_context_budget`) → housekeeping micro-slice after Slice 3". Under default shell env (where `ADR_EDITORIAL_FIX=1` leaks from the Claude Code harness) `integration_gate.py` halts at one of these. Under `env -u ADR_EDITORIAL_FIX`, all 565 unit + integration tests pass.

Sweep #18 Finding §1 already diagnosed this at `checks/reversibility-guard.sh:81-84`: the guard consumes `ADR_EDITORIAL_FIX=1` and relaxes its body-edit block; tests that simulate the block's rejection therefore see exit 0 instead of exit 2. This is an env-consumption defect in the guard, not a test defect.

**Class:** `pre-existing` (landed before 2026-04-18); now definitively attributed to `reversibility-guard.sh:81-84`.

**Fix candidate (v1-defense):** `v1-defense-d3/reversibility-guard-env-scope` — either require an additional process-level sentinel (e.g. a specific stdin token the CLI operator must pass) alongside `ADR_EDITORIAL_FIX`, or scope consumption to a narrower context check. Root-cause fix; obsoletes the "7-failure housekeeping" direction.

**Handoff action:** drop the "7 pre-existing test failures" pending item; replace with pointer to sweep #18 Finding §1 or this §2 and the proposed `v1-defense-d3/reversibility-guard-env-scope` slice.

### 3. Pre-slice pipeline-substrate drift: 2 plan docs + 1 spike doc — **expected, classified**

`snapshot_diff.py --diff` flagged 13 paths. Triaged against Slice 1 intent envelope (recovered from `git show f488801^:.claude/current-slice/intent.md`):

| Path | Status | Classification |
|---|---|---|
| `scripts/slice_orchestrator.py` | IN envelope | Slice 1 core work |
| `tests/unit/test_dispatch_contract_separation.py` | IN envelope | Phase 2 RED test |
| `tests/unit/test_multiline_json_tail.py` | IN envelope | Phase 2 RED test |
| `tests/unit/test_role_guard_wired.py` | IN envelope | Phase 2 RED test |
| `tests/unit/test_orchestrator_live_stderr.py` | IN envelope | Phase 2 RED test |
| `tests/integration/test_compressed_slice_end_to_end.py` | IN envelope | Phase 2 RED test |
| `docs/plans/2026-04-20-a1-spike-results.md` | IN envelope | Spike record (Phase 2 amendment) |
| `tests/integration/__init__.py` | OUT — `slice-caused (C)` | New test-package init; necessary ripple for Phase 2 RED; not declared in envelope. Already in d3-bypasses.log tail. |
| `tests/unit/test_housekeeping_post_slice_a_tidy.py` | OUT — `slice-caused (C)` | Updated during Slice 1; already in d3-bypasses.log tail. |
| `tests/unit/test_orchestrator_hardening.py` | OUT — `slice-caused (C)` (DELETED) | Ripple from Task 1.5 dispatch_agent contract removal; already in d3-bypasses.log tail. |
| `tests/unit/test_slice_orchestrator_state_machine.py` | OUT — `slice-caused (C)` (DELETED) | Ripple from Task 1.5; already in d3-bypasses.log tail. |
| `docs/plans/2026-04-20-compression-pipeline-hardening-design.md` | OUT — `pre-existing` | Landed pre-Slice-1 as pipeline-substrate commit `dc5dc82` |
| `docs/plans/2026-04-20-compression-pipeline-hardening-plan.md` | OUT — `pre-existing` | Landed pre-Slice-1 as pipeline-substrate commit `9769a10` |

Baseline refreshed at tip `5a8bdcd`. All 13 paths now in snapshot; re-run exits 0.

### 4. Slice #20's 5 pre-existing failures — **fully cleared**

Sweep #20 identified 5 real pre-existing failures under clean env:
- 2× `test_adr_rename_sweep` (stale 12-ADR hardcoded list)
- 2× `test_slice_orchestrator_state_machine` (slice.yaml coupling)
- 1× `test_housekeeping_post_slice_a_tidy::test_item_8_dogfooding_findings_section_in_sweep_notes` (ephemeral-path assertion)

All five are now green under clean env. Delivery:
- `test_adr_rename_sweep` → fixed by `identifier-scheme/rename-sweep-test-robust` (`c5007c4`, queued in sweep #20 Finding §1).
- `test_slice_orchestrator_state_machine` → file deleted in `compression/slice-1-foundation` Task 1.5 (ripple from dispatch_agent contract removal); `test_orchestrator_hardening` also deleted.
- `test_housekeeping_post_slice_a_tidy::test_item_8_dogfooding_findings_section_in_sweep_notes` — the test file was updated in Slice 1 (snapshot_diff "changed"); now passes.

### 5. V6b `CAIRN_LIVE_E2E=1` gate — **still valid, Slice 2 is the proof**

`handoff.md:16` gates the orchestrator end-to-end live run on `CAIRN_LIVE_E2E=1`. No change to this state in the 3-slice window. Slice 2's autonomous run IS the empirical validation per handoff; no preemptive action required at sweep time. Keep in pending.

---

## Staleness summary (handoff Blocked/Pending audit)

| Handoff item | Status | Action |
|---|---|---|
| Sweep overdue (3 slices since `housekeeping/post-slice-a-tidy`) | **Resolved** | This sweep clears it; update `sweep.yaml.last-sweep-at-slice-id` to `compression/slice-1-foundation`. |
| V6b live-E2E gated `CAIRN_LIVE_E2E=1` | Still valid | Keep; Slice 2 orchestrator run is the empirical proof. |
| 7 pre-existing test failures (`test_hook_relpath_bypass` + `test_hook_tolerance` + `test_context_budget`) → housekeeping micro-slice after Slice 3 | **Stale** | Env-leak artifacts (sweep §2). Replace with pointer to `v1-defense-d3/reversibility-guard-env-scope` as the root-cause fix; drop "after Slice 3" sequencing. |
| 3 orchestrator bugs surfaced during close → see sweep-notes bundle §Orchestrator bugs surfaced | **Pointer dangling** | Bundle not in git (sweep §1). Either retrodoc (housekeeping/slice-1-phase-4-retrodoc) or explicitly mark as lost and re-surface in Slice 2 if encountered. |
| 2 D3 bypasses logged for Slice 1 | Valid | `.claude/d3-bypasses.log` lines 16-17. No design-review trigger armed (0 false-positive entries in rolling 10). |

---

## Follow-up slice candidates (for operator triage)

1. **`housekeeping/slice-1-phase-4-retrodoc`** — recover/commit Slice 1's Phase-4 bundle from `.claude/orchestrator-debug/*.log`, reflog, or retrospective audit. Small; unblocks Slice 2 context clarity. Priority: medium.
2. **`v1-defense-d3/reversibility-guard-env-scope`** — root-cause fix for the `ADR_EDITORIAL_FIX` env-leak (`checks/reversibility-guard.sh:81-84`). Obsoletes the "7-failure housekeeping" direction. Priority: medium (currently masks real failures under default env but clean-env passes).
3. **Slice 2 queue remains** — `compression/slice-2` per `docs/plans/2026-04-20-compression-pipeline-hardening-plan.md §2.0`. No integration-sweep-level blocker.

No new firm ADRs required. All findings resolvable via new slices per rule 9.
