# Integration Sweep #13 — post-SLICE-018 batch (parallelism-collision merge)

**Date:** 2026-04-16
**Slices since last sweep (#12):** 4× SLICE-018 closed concurrently on separate branches (manual Axis-B dogfood). ADR-007 D2 parallel-SLICE-ID collision expected and observed.
**Previous sweep:** #12 at SLICE-017 (`.claude/sweep-results/2026-04-16-sweep-12.md`, commit `87ea8b5`)
**Verdict:** PASS on both merged branches independently; merge reconciliation in progress.

## Parallelism collision summary

Four worker branches all independently claimed SLICE-018 and sweep #13 per ADR-007 D2 (no global SLICE-ID pointer). Each ran `/integration-sweep` on its own branch, producing a local sweep-13.md verdict. This document is the **reconciled** merge-time sweep — it concatenates the branch-local sweep findings, tagged by source branch.

Merge order: **D** first (`e720e6a`), **B** second (this merge). A and C pending.

---

## §D — Worker D (slice/identifier-scheme-hook-relpath) sweep verdict

**Slices:** SLICE-018 (identifier-scheme/hook-relpath-bypass, complete at `19b31c1`)
**Verdict:** PASS — hook-relpath normalization lands cleanly; envelope tight; identifier-scheme feature drained; 4 carry-over stale items (1 from #12 resolved by SLICE-018).

### Failure Modes Enumerated (Pre-Check)

| Mode | Hypothesis | Outcome |
|---|---|---|
| Import conflicts | None — SLICE-018 touched shell (`checks/reversibility-guard.sh`) + one new test file | Confirmed none |
| Schema drift | None — no ADR/feature/slice schema edits; feature file append-only entry for SLICE-018 | Confirmed none |
| Tool contract breaks | Risk: path-normalization in `reversibility-guard.sh` could over-strip and allow non-ADR writes. INV-005 pre-flight evidence ruled out; re-checked here. | No regression — V1–V12 covered `.slice-system/` stripping, bare-relative, absolute, symlink-resolved paths |
| Config conflicts | None — uv.lock untouched | Confirmed none |
| Boundary violations | Envelope = `checks/reversibility-guard.sh` + `tests/unit/test_hook_relpath_bypass.py` | Diff stayed strictly in envelope (snapshot_diff clean) |
| Invariant interactions | INV-005: hook tolerates legacy + flat-slug AND now tolerates relpath + `.slice-system/`-prefixed forms | PASS |
| Adjacent-hook drift | `scope-guard.sh:53` also strips `.slice-system/` literal prefix — does SLICE-018's change split the two hooks' normalization behavior? | No — `scope-guard.sh` untouched; `reversibility-guard.sh` now normalizes independently via its own relpath logic; no shared-code dependency |
| D3 bypass window | SLICE-018 logged zero bypasses | Rolling 10 = slices 009-018; `false-positive`-class count = 0; no design-review trigger |

### Gate Results

| Check | Result | Notes |
|---|---|---|
| `integration_gate.py` Step 3 (invariants) | PASS | 7 invariants file-checked |
| `integration_gate.py` Step 4a (ruff) | PASS | clean |
| `integration_gate.py` Step 4b (pytest) | PASS | 290 passed / 1 skipped; 40 new hook-relpath tests included |
| `snapshot_diff.py --diff` | PASS | Clean — baseline already refreshed at close commit `19b31c1`; no drift since |

### Handoff Staleness Check

| Entry | Status |
|---|---|
| `docs/ARCHITECTURE.md:46` "22k token budget" | STILL STALE |
| `tests/unit/test_context_budget.py:103` docstring "≤22,000 tokens" | STILL STALE |
| Legacy bypass log reclassification (SLICE-012/014/016) | STILL STALE |
| identifier-scheme follow-on: `reversibility-guard.sh` relative-path bypass | **RESOLVED by SLICE-018 (D)** |
| identifier-scheme follow-on: `validate_architecture.py` flat-slug widening | STILL PENDING |
| d3-bypass-classification substrate slice | STILL PENDING |

### Next-Slice Recommendations (D-branch view)

1. **Cleanup slice** — `docs/ARCHITECTURE.md:46` + `tests/unit/test_context_budget.py:103` rebaseline 22k → 30k. (Note: now resolved by worker A's SLICE-018 once merged.)
2. **d3-bypass-classification legacy reclassification** — (resolved by worker B's SLICE-018, this merge).
3. **d3-bypass-classification substrate slice** — implement schema + envelope exemptions per ADR.
4. **identifier-scheme/validator-widening** — `validate_architecture.py` flat-slug recognition. (In progress as worker C's SLICE-018.)
5. **v1-defense-d2** SLICE-010/011.
6. **v1-defense-d3** substrate slice.

---

## §B — Worker B (slice/v1-defense-d3-log-reclass) sweep verdict

**Slices:** SLICE-018 (v1-defense-d3/bypass-log-reclass, complete at `6f6fa75`)
**Verdict:** PASS — all gates green; one chronic dirty file carried forward per prior operator directive; four follow-up items carry forward unchanged.

### Failure Modes Enumerated (Pre-Check)

| Mode | Hypothesis | Outcome |
|---|---|---|
| Import conflicts | None — SLICE-018 touched only `.claude/d3-bypasses.log` + a new test file | Confirmed none |
| Schema drift (`d3-bypasses.log`) | SLICE-018 changed log format from free-text to classified. Consumers: `scripts/snapshot_diff.py` (not a parser, append-only consumer — safe); `commands/claude-code/start-slice.full.md:224` (rolling-window rule text still says "3+ bypasses" all-class; should say "false-positive only") | Parser-side clean; `start-slice.full.md:224` carry-forward confirmed |
| Tool contract breaks | Hook deps (jq, ruff) unchanged | Confirmed none |
| Config conflicts | `sweep.yaml` bumped `current-slice-number` 17→18; no schema change | Clean |
| Boundary violations | Known: `tests/unit/test_d3_bypass_log_format.py` out-of-envelope (EXPAND_ENVELOPE=1, Phase 2); log-in-place impossible without breaking SLICE-018's own Phase 2 contract; logged OOB at `.claude/slice-018-d3-oob.md` | Resolved at sweep time by refreshing snapshot baseline; OOB file persists until Decision 2 slice retires it |
| Invariant interactions | INV-004 rebaselined at SLICE-017; SLICE-018 touched no command/command-body file, so budget stable | PASS (28878 turn-1, under 30k hard) |
| Provisional ADR drift | `d3-bypass-classification` cited correctly as governing; not treated as firm | Clean |

### Gate Results

| Gate | Command | Exit | Detail |
|---|---|---|---|
| D1 refresh | (run at SLICE-018 close) | 0 | ARCHITECTURE.md consistent with 10 ADRs |
| Architecture validator | `uv run python scripts/validate_architecture.py` | 0 | 7 invariants, 9 ADRs, PASS |
| Integration gate | `uv run python scripts/integration_gate.py` | 0 | Step 3 PASS · Step 4a (ruff) PASS · Step 4b (pytest) PASS |
| Snapshot diff (pre-refresh) | `uv run python scripts/snapshot_diff.py --diff` | 1 | One out-of-envelope path: `tests/unit/test_d3_bypass_log_format.py` (new, Phase 2 EXPAND_ENVELOPE=1 artifact) |
| Snapshot refresh | `uv run python scripts/snapshot_diff.py --snapshot` | 0 | Baseline regenerated |
| Snapshot diff (post-refresh) | `uv run python scripts/snapshot_diff.py --diff` | 0 | Clean |

### D3 bypass rolling-window check

`.claude/d3-bypasses.log` post-SLICE-018 state: four `pre-existing:` entries (SLICE-012/014/016/017) in the rolling 10-slice window. `false-positive` count = **0/10**. No trigger.

### Follow-up (coordinator input)

1. **Parallelism merge reconciliation** — resolved at this merge commit (coord branch is reconciling).
2. **Carry-forward unchanged** — all 5 pre-sweep Blocked/Pending items remain valid.
3. **`.claude/slice-018-d3-oob.md` retirement** — once the Decision 2 `exempt:` syntax slice lands.
4. **Handoff directive propagation** — "chronic leftover, ignore" measurement-file directive applies to this branch too.

---

## Reconciliation note (merge author: coord)

- Both D and B ran `/integration-sweep` on their branches and each bumped `.claude/sweep.yaml` to `last-sweep-at-slice: 18`. On merge, sweep.yaml auto-merged (identical target value). Sweep-13.md files are text-conflicting and reconciled here.
- ADR-007 D2 prediction (parallel SLICE-ID + sweep-ID collision is safe at merge time) is **confirmed for this pair of workers**. Reconciliation cost at merge: ~minutes to hand-merge the sweep-13.md and handoff.md; no content loss.
- Pending merges: worker A (stale-22k-cleanup) and worker C (validator-flat-slug) will add further SLICE-018 branches. Expect the same reconciliation shape; append further `§A` and `§C` sections to this file on their merges.
