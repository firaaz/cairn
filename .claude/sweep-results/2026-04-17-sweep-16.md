# Integration Sweep #16 — post-SLICE-021 + SLICE-022 (two-slice window)

**Date:** 2026-04-17
**Branch:** `feature/identifier-scheme`
**Tip:** `cb1f5d9`
**Previous sweep:** #15 (`2026-04-17-sweep.md`) — post-SLICE-020 configurable-pytest-timeout, PASS with 1 carried finding (`test_d3_bypass_log_format` brittleness)
**Verdict:** **PASS — cleanest window in several sweeps.** Both in-window slices landed Phase 4 clean. Prior carried finding (`test_d3_bypass_log_format`) resolved by SLICE-021. Integration gate fully green; no bypasses; ADR rename sweep landed without straggler references in live paths.

---

## Cross-slice failure modes (pre-check enumeration)

| Mode | Hypothesis | Outcome |
|---|---|---|
| D3 bypass log test resilience holdover | SLICE-021 rewrote the suite; did format regex + line-count logic generalize correctly, and does pytest stay green under the current 7-line log state? | Clean — `integration_gate.py` pytest step exits 0; prior 4/4 failures absent |
| ADR rename sweep correctness (INV-005) | SLICE-022 performed 9 file renames + frontmatter `id:` migration + cross-reference sweep; any straggler reference in live paths would break cross-slice cites | Clean — 9/9 renames visible in diff stat; no live NNN-slug.md references outside declared historical/test-fixture scope (see Finding #2) |
| Validator recognition of renamed corpus | `scripts/validate_architecture.py` must resolve both flat-slug and legacy NNN-slug, and must still walk the post-rename corpus | Clean — integration_gate Step 3 (Check D) exits 0; all 7 invariants verified |
| Hook path tolerance under rename window | `reversibility-guard.sh` / `scope-guard.sh` / `reality-check.sh` must accept both filename shapes for ADR writes | Clean — SLICE-022 Phase 3 committed 9 ADR renames without a hook denial or bypass (log untouched) |
| ADR frontmatter `id:` drift | Each renamed file's `id:` field must match the new flat slug; validator Checks B/C/D depend on this | Clean — validator PASS |
| Handoff budget regression | Two slice closes + sweep-queue handoff rewrites could accumulate prose | Clean — post-close handoff at 1355 chars, well under 2000 |
| d3-bypass log growth | New bypass events during window would widen the log and re-expose resilience gaps | None — `.claude/d3-bypasses.log` untouched since pre-window (7 lines, no new entries) |
| Feature-file consistency | `.claude/features/identifier-scheme.yaml` must reflect SLICE-022 landing | Preserved — append-only entries; feature not yet drained (two queued slices remain) |

---

## Gate Results

| Gate | Command | Exit | Detail |
|---|---|---|---|
| D1 refresh + validator | `refresh-architecture` skill + `validate_architecture.py` | 0 | PASS — no ARCHITECTURE.md changes; 7 invariants verified; 11 active ADRs |
| Integration gate — Step 3 | `integration_gate.py` | 0 | PASS — invariant Check D |
| Integration gate — Step 4a | `integration_gate.py` | 0 | PASS — ruff |
| Integration gate — Step 4b | `integration_gate.py` | 0 | PASS — pytest (includes previously-failing `test_d3_bypass_log_format` suite, now green) |
| Snapshot diff | `snapshot_diff.py --diff` | 0 | Clean (baseline refreshed during SLICE-022 D3-B close at `f4a6c6b`) |

Overall: fully green. First sweep in the window series with `test_d3_bypass_log_format` not flagged.

---

## Findings

### Finding #1 — Sweep #15 Finding #1 (`test_d3_bypass_log_format` brittleness) RESOLVED

SLICE-021 (`v1-defense-d3/bypass-log-test-resilience`) rewrote the suite: `len == 4` replaced with format-per-line checks; regex widened to accept optional `(A|B|C|D)` class-qualifier prefix. Integration gate Step 4b now exits 0; the full log (7 entries at sweep time) parses cleanly.

Evidence: `tests/unit/test_d3_bypass_log_format.py` diff in window (`85 +++-`); pytest PASS.

### Finding #2 — Residual `NNN-slug.md` references are all intentional (test fixtures + frozen docs)

Grep for `\b0[0-9]{2}-[a-z-]+\.md\b` across live paths surfaces:
- `tests/unit/test_hook_relpath_bypass.py:62-64` and `tests/unit/test_hook_tolerance.py:48` — `LEGACY_*` fixtures exercising hook tolerance for legacy filename shape. Intentional; tolerance window is explicitly preserved until Phase 3 drop (deferred indefinitely per `identifier-scheme` D7).
- `tests/unit/test_adr_rename_sweep.py:43-68` — the rename-sweep test itself, referencing pre-rename filenames as source expectations. Intentional.
- `tests/unit/test_validate_architecture.py:478` — `"filename": "001-legacy.md"` fixture testing legacy-format recognition. Intentional.
- `docs/spec-v1.md:261` — "Incident 2" narrative text referencing a historical proposed ADR file (`008-universal-rag-mcp-server-architecture.md`). Historical prose, not a live cross-reference.
- `docs/plans/**` — explicitly out-of-scope per SLICE-022 intent (frozen historical sites).

None represent cross-reference drift. No action.

### Finding #3 — Prior staleness + non-staleness (carry-over)

- `docs/plans/measurements/2026-04-12-slice-003.txt` uncommitted — session-start hook auto-drift, operator directive to ignore. No action.
- Feature-1 rename queue — `adr-rename-sweep` closed this sweep; `slice-and-feature-rename` + `doc-sweep` remain queued per feature file.
- d3-bypass-classification Decision 2 `exempt:` syntax — STILL PENDING (carried from #14, #15).
- `snapshot_diff.py` classified-format parser — STILL PENDING (carried).
- `start-slice.full.md:220` rolling-window wording — STILL PENDING; text says "slice-id numeric suffix" but identifier-scheme IDs are hierarchical. Not blocking (current bypass entries all use legacy SLICE-NNN shape, so slug-suffix parsing still works).
- 5 SLICE-020 reviewer suggestions → `docs/lessons.md` — STILL PENDING (grep confirmed not landed).

---

## Multi-Module / Workaround Review (git log window: `2fe7ef3..HEAD`)

21 commits total. Two slices.

- **SLICE-021** (`6ae4de4 → 7b5ed12`, 4 slice commits) — envelope `tests/unit/test_d3_bypass_log_format.py`. Narrow, test-only edit. Clean.
- **SLICE-022** (`0a63f39 → f4a6c6b`, 4 slice commits) — legitimately multi-module per declared envelope: `docs/adr/` (9 renames + `id:` migrations), `docs/adr/index.md`, `docs/ARCHITECTURE.md`, `docs/operational-reference.md`, `docs/spec-v1.md`, `docs/lessons.md`, `commands/claude-code/*.md`, `scripts/validate_architecture.py`, `scripts/dogfood_evaluate.py`, live tests under `tests/unit/`, `.claude/features/*.yaml`, `CLAUDE.md`. Diff stat 48 files / 1160 insertions / 505 deletions — matches the intent's declared sweep scope.
- **No `ADR_*_BYPASS` / `D3_GATE_BYPASS` invocations** — `.claude/d3-bypasses.log` untouched; no D1 bypass events.
- **No provisional-as-firm ADR citations** — `cliff-failure-mode-and-v1-defenses`, `parallelism-v1`, `d3-bypass-classification` remain provisional; `identifier-scheme` cited firm in two slices, which is correct per its status line.
- **Handoff edit rate** — ~10 handoff commits in window (one per phase boundary × 2 slices + 2 top-level rewrites). Normal cadence.

---

## Handoff Staleness Check

Cross-referenced `.claude/handoff.md` Blocked/Pending + Features against git history:

| Entry | Status |
|---|---|
| Sweep #16 due | **IN PROGRESS** (this sweep) — will clear on commit |
| `test_context_budget::test_inv004_turn1_token_budget` CC-drift flake | **STILL PENDING** — operator directive to ignore |
| `docs/plans/measurements/2026-04-12-slice-003.txt` uncommitted | **STILL PENDING** — operator directive to ignore |
| `identifier-scheme` queued: slice-and-feature-rename, doc-sweep | **ACCURATE** — both queued per feature file |
| `v1-defense-d3` Decision 2 substrate queued | **ACCURATE** |
| `integration-gate: complete` | **ACCURATE** |
| `housekeeping: complete (SLICE-017/018)` | **ACCURATE** |
| `v1-defense-d2: complete (SLICE-010/011)` | **ACCURATE** — post-#15 correction now in place |
| `v1-defense-d3: bypass-log-test-resilience complete; Decision 2 substrate queued` | **ACCURATE** |

No staleness in current handoff. The `v1-defense-d2` staleness flagged by sweep #15 was corrected in subsequent handoff edits.

---

## Next-Slice Recommendations (priority order)

1. **`identifier-scheme/slice-and-feature-rename`** — next in queue per feature file; D7 Phase 2 Part 2.
2. **`identifier-scheme/doc-sweep`** — D7 Phase 2 Part 3 (residual prose sweep).
3. **d3-bypass-classification Decision 2 `exempt:` syntax** — carried from #14, #15. No movement across this window.
4. **`snapshot_diff.py` classified-format parser** — carried from #14, #15.
5. **5 SLICE-020 reviewer suggestions → `docs/lessons.md`** — process carry-over; coincident with any docs-touching slice.
6. **`start-slice.full.md:220` rolling-window wording** — text fix for hierarchical slice IDs; low priority, not blocking any gate.

---

## Sweep Output

- Report: this file (`.claude/sweep-results/2026-04-17-sweep-16.md`)
- `.claude/sweep.yaml` → `last-sweep-at-slice: 22` (bumped from 20; covers SLICE-021 + SLICE-022)
- Snapshot baseline: refreshed during SLICE-022 D3-B close at `f4a6c6b` (no fresh `--snapshot` needed; `--diff` already clean against the post-close baseline)
