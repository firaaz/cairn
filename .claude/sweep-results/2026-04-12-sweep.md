---
date: 2026-04-12
sweep-number: 2
last-sweep-at-slice: 1 (bb46383)
current-slice-number: 3
slices-covered: [SLICE-002 (archived), SLICE-003-precursor]
verdict: PASS-with-known-debt
---

# Integration Sweep — 2026-04-12

Second cross-slice sweep for cairn. Runs after SLICE-003-precursor reached `status: complete` at `79be5a7`. Overdue by one slice: `sweep.yaml` shows `last: 1`, `interval: 1`, `current: 3` — threshold (`last + interval = 2`) was crossed at slice 2 and remained exceeded at slice 3. SLICE-002 is archived (not superseded) to `.claude/completed-slices/SLICE-002-stopped/`, so this sweep's substantive coverage is SLICE-003-precursor; SLICE-002's envelope is preserved but did not land.

## Scope and context

Cairn remains a methodology repo — no runtime application, no service, no module graph beyond `scripts/validate_architecture.py` and three shell hooks. The substrate is markdown protocols, which means most cross-slice failure classes are **protocol-contract drift** (string-match coupling between skills and hook allowlists) rather than schema/import breakage. The enumeration in Step 2 below reflects this reality; Steps 3 and 4 probe what actually exists.

ARCHITECTURE.md freshness: **current.** Last refresh `7cfe719` (2026-04-11 22:59) is newer than the latest ADR edit `cba46f9` (2026-04-11 22:56). No `/refresh-architecture` required before this sweep.

## Step 2 — Cross-slice failure mode enumeration

| # | Failure class | Potential impact | Check location |
|---|---|---|---|
| F1 | Phase-name string drift | INV-003 coupling across /catchup, /start-slice, hooks, slice.yaml status | grep "Intent\|Validation\|Implementation\|Integration" |
| F2 | Role-name drift (Reader/Skeptic/Builder/Auditor) | Phase Skill Guide cross-ref to skills breaks silently | Phase Skill Guide § vs /catchup vs /start-slice vs ADR-004 |
| F3 | ADR index row vs filesystem divergence | INV→ADR grounding broken; validator misfires | docs/adr/index.md vs ls docs/adr/ |
| F4 | Handoff.md structural / token-bound drift | INV-002 (a) violation | .claude/handoff.md vs handoff.md skill protocol |
| F5 | Catchup five-item list drift | INV-002 (b) violation | commands/claude-code/catchup.md |
| F6 | Slice-close wipe missing | INV-002 (c) violation — two slices share residue | commands/claude-code/start-slice.md Step 7 |
| F7 | Hook allowlist vs protocol divergence | scope-guard / reality-check / reversibility-guard silently over- or under-block | checks/*.sh |
| F8 | Symlink recursion (`.slice-system → .`) | `find`/`rglob` hangs or infinite loops | grep for glob patterns in scripts/ |
| F9 | Sweep-report persistence vs INV-002 (c) wipe | Report written to current-slice lost on slice-close | integration-sweep.md Step 6.5 routing |
| F10 | Provisional ADR-003 D1/D2/D3 treated as firm | Pre-v1 freeze violation | grep for d1-bypasses.log, D1/D2/D3 in runtime code |
| F11 | Phase Skill Guide registry drift from ADR-004 | Future Reader told stale role anti-behaviors | operational-reference.md § Phase Skill Guide vs ADR-004:50–76 |
| F12 | `adrs-referenced` D3 gate vs filesystem | Gate misfires on uncommitted ADR paths | start-slice.md Step 3 |

## Step 3 — Per-invariant check

| INV | Statement (abbrev.) | Status | Evidence |
|---|---|---|---|
| 001 | All post-bootstrap dev flows through /decision or /start-slice | **OBSERVATION** (scar-continued) | 16 non-slice commits between `bb46383..HEAD` (plan docs, lessons updates, ARCHITECTURE refreshes, settings, chore archive). Of these, only `7cfe719` and `13bebde` (`docs: refresh ARCHITECTURE.md from ADRs`) are explicitly authorized by L-001 as pipeline-substrate scars. The remaining 14 commits are in the same scar class that L-001 names as "not a license" — direct docs commits that should have been carried in handoff state or packaged into slices. **Not a regression** (same class as the 2026-04-11 sweep finding) and **not blocking** (maintainer-discipline barrier is the only enforcement pre-D2); recorded as ongoing debt that D2's code↔invariant binding is expected to eventually machine-check. |
| 002 | Three-layer context discipline (a/b/c) | **FAIL — known debt (SLICE-002 scope)** | See INV-002 detail below. All three sub-clauses unenforced at protocol level. This is exactly the scope of SLICE-002 (archived to `.claude/completed-slices/SLICE-002-stopped/`). The P0b resolve/supersede decision is still owed per `docs/plans/2026-04-11-adr-003-v1-execution-plan.md:48`. |
| 003 | Four phases/roles locked + surfaced + D3 gate | **PASS** | Four phases/roles locked: `docs/ARCHITECTURE.md:16,27`, `docs/operational-reference.md:82–85`, `docs/adr/004-*.md:52–55`. Phase Skill Guide section exists: `docs/operational-reference.md:74`. Phase-entry surfacing: `commands/claude-code/catchup.md:69` (Mode A, post-load, pre-orientation), `commands/claude-code/start-slice.md:43` (Step 3, post-gate, pre-phase-body). D3 `adrs-referenced` gate: `commands/claude-code/start-slice.md:33` (table row), `:37` (slug semantics), `:39` (plurality commitment). Pytest corroboration: `tests/unit/test_slice_003_precursor.py` 7/7 PASS. Validator: 3 invariants verified, exit 0. One known-drift observation carried forward from Phase 4 Auditor (see Observations). |

### INV-002 detail

**(a) handoff.md bounded 150–400 tokens with fixed section structure and forbidden-sections list.**
- Protocol file `commands/claude-code/handoff.md` has no token-bound instruction, no forbidden-sections list (grep for `150|400|token|forbidden` returns zero matches). Section structure is specified (Step 2) but size/discipline is not.
- Live evidence of violation: current `.claude/handoff.md` is 949 words (~1200+ tokens), ~3× the 400-token ceiling. Section structure is honored; size bound is not.
- Status: protocol-level enforcement missing; SLICE-002 was chartered to fix this.

**(b) /catchup reads a fixed five-item list into main context; Tier 2 reads gated via subagent.**
- Protocol file `commands/claude-code/catchup.md` has no "fixed five-item list" cap. Mode A for Phase 1 loads: `handoff.md`, `slice.yaml`, `handoff-phase-N.md`, `ARCHITECTURE.md`, `adr/index.md`, `operational-reference.md § Phase Skill Guide`, and phase-declared inputs (public interfaces for modification slices) — already 6–8 items before Tier 2.
- No subagent-dispatched Tier 2 admission criteria appear anywhere in the skill body.
- Status: protocol-level enforcement missing; SLICE-002 was chartered to fix this.

**(c) /start-slice wipes `.claude/current-slice/` on transition to `status: complete`.**
- Protocol file `commands/claude-code/start-slice.md` Step 7 "Complete a Slice" (lines 182–194) only updates `slice.yaml → status: complete`, commits, runs `/handoff`. No wipe step.
- Step 4 "Initialize New Slice" (lines 91–127) writes `slice.yaml` and directory structure for a new slice but does not explicitly clear pre-existing residue first — it overwrites `slice.yaml` but not sibling files.
- Live symptom: SLICE-003-precursor's `current-slice/` still contains `intent.md`, `validation/`, `implementation/`, `integration/`, `handoff-phase-{1,2,3}.md`, and a stale `slice.yaml status: complete` — all legitimate archival state, but not wiped. The next `/start-slice` will inherit this residue unless the maintainer manually clears it.
- Status: protocol-level enforcement missing; SLICE-002 was chartered to fix this.

**All three sub-clauses fail. This is NOT a regression** — it is the pre-SLICE-002 status quo. SLICE-002's archive (`.claude/completed-slices/SLICE-002-stopped/`) preserves the Phase 2 validation work (test design for INV-002 machine-checking under Approach 1+). The P0b resolve/supersede review is still owed and blocks SLICE-003 (D1) start per the execution plan.

## Step 4 — Cross-module / substrate checks

| Check | Command | Result |
|---|---|---|
| Architecture validator | `uv run python scripts/validate_architecture.py` | **PASS** — 3 invariants verified, 4 ADR files checked, exit 0 |
| Ruff lint | `uvx ruff check scripts/ tests/` | **PASS** — all checks passed |
| Pytest suite | `uv run --with pytest python -m pytest tests/ -x --tb=short` | **PASS** — 13/13 in 0.50s (7 SLICE-003-precursor + 6 pre-existing test_validate_architecture) |
| Hook shell syntax | `bash -n checks/*.sh` | **PASS** — all three hooks parse |
| Hook dependencies | `which jq ruff` | **PASS** — both installed at `/opt/homebrew/bin/` |

No runtime modules under `src/` to import-check; cairn has no `src/` tree. Import integrity is N/A.

## Step 5 — History review

SLICE-003-precursor envelope (`docs/operational-reference.md`, `commands/claude-code/catchup.md`, `commands/claude-code/start-slice.md`, `tests/unit/test_slice_003_precursor*.py`) was honored exactly across all four phases. No out-of-envelope file touches. Three-file coupling across the slice is by-design (shared Phase Skill Guide contract surfaced from both skills) — a deliberate protocol contract, not accidental coupling.

No workaround / temporary-fix commit messages visible in `bb46383..HEAD`. ADR-003 remains provisional; no hook or script in the repo takes a hard dependency on D1/D2/D3 landing (`grep d1-bypasses.log checks/ scripts/` → no matches). F10 clean.

`8ce3178 chore: archive SLICE-002 to backburner` is housekeeping, not a protocol violation: it moves stopped artifacts to `completed-slices/` and frees `current-slice/` for the precursor. The P0b review is explicitly deferred, not forgotten.

## Observations (non-blocking)

1. **Phase Skill Guide D4 drift at `operational-reference.md:96`.** ADR-004:76's Phase 4 notes cell contains the sentence *"Code-reviewer subagent dispatched via `requesting-code-review` is the external check for invariant verification"*. This sentence was dropped when the D4 table was lifted into `docs/operational-reference.md:96` during SLICE-003-precursor Phase 3. The primary-skill mapping itself (`requesting-code-review`) is preserved, but the operational rationale is lost in the registry copy. The Phase 4 Auditor flagged this in `.claude/completed-slices/SLICE-002-stopped/`... correction: in the Phase 4 sweep-notes of SLICE-003-precursor. Repair is an ordinary doc edit; carry-over from the prior sweep report.

2. **`dispatching-parallel-agents` exclusion wording at `operational-reference.md:102` is wrong.** Current text: *"parallelism-native work violates v1 single-slice discipline per ADR-003 D4 (Vision commitment #2 parallelism-native is time-boxed to v2+)"*. The v2+ time-box in ADR-003 D4 applies to **split-agent / concurrent-worktree slices** (two slices running side-by-side), NOT to **same-session subagent dispatch inside a single phase** (e.g., Phase 3 dispatching multiple Builder subagents against independent envelope files). Within-slice parallelism is in scope for v1 and is often the right choice. The exclusion should either be removed entirely or reframed to target only cross-slice parallelism. Surfaced by the maintainer during this sweep; logged here for repair rather than fixed inline because the sweep is non-modifying by design. *This also suggests the existing note at `operational-reference.md:95` mapping `subagent-driven-development` as a Phase 3 supporting skill is correct and should be strengthened, not hedged.*

3. **SLICE-002 P0b resolve/supersede debt.** `docs/plans/2026-04-11-adr-003-v1-execution-plan.md:48` explicitly declares this as a prerequisite for SLICE-003 (D1) start. The archive at `.claude/completed-slices/SLICE-002-stopped/validation/approach.md` preserves the Phase 2 test design for INV-002 machine-checking; the resolve decision has two plausible outcomes (amend-and-resume contributing to D2, or supersede with a successor ADR). This is **Tier 0** priority under the execution plan.

4. **Non-slice commit rate is elevated.** 14 scar-class direct commits in the `bb46383..HEAD` window is more than the 1 direct commit that triggered L-001 between bootstrap and the first sweep. Not a regression in structural sense (L-001 explicitly names the scar-continue posture) but worth noting as a dogfood signal: the maintainer-discipline barrier is bending more often, which increases the expected-value of the D2 machine-checking defense.

## Invariant-tension check

F9 (sweep report persistence vs INV-002 (c) wipe): the `/integration-sweep` skill at Step 6.5 routes the report to `.claude/sweep-results/<date>-sweep.md` when no active slice exists, escaping the `current-slice/` wipe. This sweep is writing to that path, confirming the escape works in practice. Non-issue.

## Verdict

**PASS-with-known-debt.**

- INV-003: clean PASS with one carry-over drift observation.
- INV-002: FAIL as stated, but entirely within the scope of archived SLICE-002 — NOT a regression, NOT a new failure, and not blocking the continuation of work on other axes. The P0b review is the path to resolution and is a Tier 0 prerequisite for SLICE-003 (D1).
- INV-001: OBSERVATION-class scar debt, documented continuation of the L-001 posture, awaiting D2 machine-check for mechanical enforcement.
- Cross-module substrate (validator, ruff, pytest, hooks, deps): all green.

No new slices are required as fallout from this sweep. The outstanding work items were already known and live in the execution plan:

1. **P0b review** (SLICE-002 resolve/supersede) — Tier 0, blocks SLICE-003 (D1) start.
2. **SLICE-003 (D1 design + implementation)** — Tier 1, on v1 critical path.
3. **Drift repair at `operational-reference.md:96` and `:102`** — ordinary doc edits; can be bundled into whichever slice next touches `operational-reference.md` (likely SLICE-003 D1) or landed as a micro-slice. Not urgent.

## Recommendations

- **Do P0b next.** It is the formal Tier 0 prerequisite for D1 and is cheap (it's a review decision, not a new slice). Outcome: either amend SLICE-002's envelope to contribute to D2 (via INV-002 machine-checking) and resume, or write a successor ADR superseding SLICE-002 outright. Preserve the Phase 2 approach.md work in either path.
- **Then SLICE-003 (D1).** Start via `/start-slice` in a fresh session. Phase 1 Reader will now inherit a live role/skill surfacing mechanism from the precursor — first real test of ADR-004 D4 in action.
- **Do not land the `operational-reference.md:96` + `:102` drift repairs as a standalone cleanup slice.** Fold them into the next slice that has a legitimate reason to touch `operational-reference.md` (most likely SLICE-003 D1's Phase 3 when the new D1 flow is documented there). Avoids a near-duplicate cleanup commit.
- **Record the :102 parallelism-scope clarification in `docs/lessons.md`** as a new lesson (L-004 candidate) **only if it recurs once more** — per the 3× rule in context-discipline philosophy. For now, the feedback memory at `~/.claude/projects/-Users-mohammed-farook-Developer-lab-cairn/memory/parallelism_scope.md` is sufficient.
- **Consider accelerating D2's design slice** if the non-slice commit rate keeps climbing. The current scar count is 14 in two slices' worth of activity; at that rate, the "maintainer discipline is the only barrier" posture looks less durable than the execution plan currently assumes.

## Sweep tracking update

Next step after writing this report:
1. Commit the sweep report under `.claude/sweep-results/`.
2. Update `.claude/sweep.yaml` → `last-sweep-at-slice: 3`.
3. Commit the sweep.yaml bump.
