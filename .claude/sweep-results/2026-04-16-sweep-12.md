# Integration Sweep #12 — post-SLICE-017 boundary

**Date:** 2026-04-16
**Slices since last sweep (#11):** SLICE-017 (housekeeping/inv004-rebaseline, complete)
**Previous sweep:** #11 at SLICE-016 (`.claude/sweep-results/2026-04-16-sweep.md`, commit `6f215aa`)
**Verdict:** PASS — INV-004 rebaseline holds at 30k on CC 2.1.110; envelope clean; two stale-doc cleanup candidates carry forward.

## Failure Modes Enumerated (Pre-Check)

| Mode | Hypothesis going in | Outcome |
|---|---|---|
| Import conflicts | None — SLICE-017 touched ARCHITECTURE.md + test_context_budget.py + uv.lock | Confirmed none |
| Schema drift | None — no ADR/feature/slice schema edits | Confirmed none |
| Tool contract breaks | None — no hook/command changes | Confirmed none |
| Config conflicts | uv.lock refresh could break pytest resolution | Pytest resolved cleanly |
| Boundary violations | Low — housekeeping envelope narrow | Diff stayed in envelope (4 files, all expected) |
| Invariant interactions | INV-004 budget 22k→30k; must pass at new ceiling | PASS |
| D3 bypass window | SLICE-017 logged one `pre-existing:` bypass — 4/10 legacy+new | New-format entry clean; legacy 3 still need reclassification |

## Gate Results

| Check | Result | Notes |
|---|---|---|
| `integration_gate.py` Step 3 (invariants) | PASS | 7 invariants file-checked |
| `integration_gate.py` Step 4a (ruff) | PASS | clean |
| `integration_gate.py` Step 4b (pytest) | PASS | `test_inv004_turn1_token_budget` now green under 30k hard budget on CC 2.1.110 |
| `snapshot_diff.py --diff` | FAIL (expected) | 5 paths flagged — all accounted for by committed work since sweep #11 |
| Baseline updated | Yes | `snapshot_diff.py --snapshot` ran to exit 0 after reconciliation |

## Out-of-Envelope Artifacts (Reconciled)

All 5 paths flagged by `snapshot_diff.py --diff` were committed between sweep #11 and HEAD; none represent unscoped drift:

| Path | Origin commit | Classification |
|---|---|---|
| `docs/ARCHITECTURE.md` | `fdc1ad1` (SLICE-017 phase 4) | In-envelope — INV-004 rebaseline paragraph |
| `docs/adr/d3-bypass-classification.md` | `7810c17` (/decision) | Intentional new ADR |
| `docs/adr/index.md` | `7810c17` | Intentional — ADR index entry for the new ADR |
| `docs/lessons.md` | `f26715c` | Intentional — L-004 lesson capture |
| `tests/unit/test_context_budget.py` | `778f558` + `fdc1ad1` (SLICE-017 phases 3/4) | In-envelope — rebaseline + regression guard |

Also in SLICE-017's diff but not flagged (pre-approved paths): `docs/plans/measurements/2026-04-12-slice-003.txt`, `uv.lock`.

## Multi-Module / Workaround Review (git log -20)

- No multi-module commits detected — every slice/decision commit is tight to its stated envelope.
- One class: `pre-existing` bypass under d3-bypass-classification (SLICE-017). Falls inside the new three-class schema; no design review trigger.
- No provisional ADR treated as firm: d3-bypass-classification still `provisional`, untouched.

## Handoff Staleness Check

Handoff `Blocked / Pending` entries cross-referenced against git history:

| Entry | Status |
|---|---|
| Legacy bypass log reclassification (SLICE-012/014/016) | STILL STALE — legacy 3 lines unchanged; SLICE-017's entry uses new `<class>:` format |
| `docs/ARCHITECTURE.md:46` "22k token budget" | CONFIRMED STALE — line still reads `description: "...machine-checks the 22k token budget"` |
| `tests/unit/test_context_budget.py:103` docstring "≤22,000 tokens" | CONFIRMED STALE — docstring unchanged; note lines 121/148/149 legitimately reference the literal as a regression assertion and must NOT be swept |
| identifier-scheme follow-ons (`validate_architecture.py` flat-slug, `reversibility-guard.sh` relative-path) | STILL PENDING — no touches since SLICE-016 |
| d3-bypass-classification substrate slice | STILL PENDING — ADR landed only, no implementation slice |

No resolved items found masquerading as pending.

## Next-Slice Recommendations

No integration blockers. Downstream queue, in priority order:
1. **Cleanup slice** — `docs/ARCHITECTURE.md:46` description + `tests/unit/test_context_budget.py:103` docstring rebaseline to 30k. Tiny envelope; could bundle with item 2.
2. **d3-bypass-classification legacy reclassification** — one-time format migration of SLICE-012/014/016 log lines (Decision 1 of the ADR).
3. **d3-bypass-classification substrate slice** — implement schema + envelope exemptions per ADR.
4. **identifier-scheme follow-ons** — `validate_architecture.py` flat-slug widening, `reversibility-guard.sh` relative-path bypass.

None blocks further slice work; all are queue items.
