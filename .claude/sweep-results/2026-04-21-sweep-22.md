# Integration Sweep #22 — post-`compression/slice-3-observability-and-close-slice` (2-slice window)

**Date:** 2026-04-21
**Branch:** `feature/compression`
**Tip:** `c050772`
**Previous sweep:** #21 (`2026-04-20-sweep-21.md`) — post-`compression/slice-1-foundation`, PASS.
**Window:** 2 slices closed since #21 — `compression/slice-2-state-machine` (close `9b34f44`) + `compression/slice-3-observability-and-close-slice` (close `e025714`), plus pre-Slice-3 ADR work (`d11277b` slice-close-contract + orchestrator-observability, `ff6a08e` ARCHITECTURE refresh, `9bc65f9` L-007 lesson) and slice-2 sweep was pre-bumped without running.
**Verdict:** **FAIL** (integration gate exits 1). All failures classified — 11 inherited pytest reds + 1 inherited ruff F841 match the handoff's queued `housekeeping/post-inv008-tech-debt` scope; 2 new E741 ruff errors were introduced by Slice 3's Phase-2 RED suite and slipped past the manual Phase-4 audit. No novel cross-slice regressions.

---

## Cross-slice failure modes (pre-check enumeration)

| Mode | Hypothesis | Outcome |
|---|---|---|
| Stdlib-only drift in new Python | Slice-3 observability writer + heartbeat thread pulls 3rd-party deps | **Clean** — `scripts/slice_orchestrator.py` stdlib-only; no new imports added beyond `threading`/`json`/`os`/`sys`/`pathlib` |
| INV-008 close-lifecycle breakage | DC-3 idempotency / DC-4 sole-commit-source / DC-5 wipe / DC-6 resume matrix / DC-7 slug isolation fail interaction | **Clean** — 9/9 `test_close_slice_hardened` + 2/2 `test_close_slice_invocation` + `test_cross_slice_isolation` (modulo E741) + `test_resume_reconcile` GREEN |
| Observability schema drift | v1.0 JSON schema additive-only property violated by future writers | **Clean** — schema-field test `test_state_schema.py` GREEN; schema v1.0 present with `worktree_path`/`orchestrator_pid` as additive |
| Heartbeat daemon leakage | CAIRN_HEARTBEAT_* env vars leak / thread not joined on close | **Clean** — `test_heartbeat` GREEN; heartbeat file wiped by `close_slice` per DC-5 |
| `.claude/orchestrator-debug/` retention tripwire regression | D8 rotation policy accidentally activated | **Clean** — no rotation code landed; D8 deferred per ADR |
| Triager misroute downstream | Slice-3 manual Phase-4 recovery left orchestrator in an unexpected state | **Clean** — `slice.yaml status: complete`, handoff committed, tree wiped (only `slice.yaml` remains in `current-slice/`), `close_slice` idempotent |
| Snapshot baseline stale across 2-slice window | Slice-2 + Slice-3 accumulation drift masking regressions | **Partial finding** — 33 out-of-envelope paths surfaced, all classified as slice-caused (C); baseline refreshed |
| Provisional ADR elevated to firm without supersession | `orchestrator-observability` (provisional) treated as firm via INV-008 attribution | **Observation §3** — INV-008's text references observability-ADR content (heartbeat advisory, schema v1.0) but only the slice-close-contract portion is machine-checked; acceptable asymmetry, flagged for awareness |
| INV-002 / INV-004 token-budget interaction | Session-start budget regression from Slice-2 or Slice-3 substrate additions | **Finding §1, reframed** — INV-004 RED at 30170-30351 tokens; root cause is CC 2.1.114 → 2.1.116 harness bump (+583 tokens), not cairn content growth |
| Multi-module commits or workaround signals in history | Hidden hacks smuggled past slice boundaries | **Clean** — no `workaround|hack|skip|xfail|tmp|todo|fixme` in commit subjects across 2-slice window |
| Hook envelope contract drift | `role_guard.py` JSON-list envelope changes since sweep-21 (`779e7f5`) break downstream consumers | **Clean** — `test_role_guard_envelope_json.py` GREEN; legacy colon-split back-compat exercised |

---

## Gate results

| Gate | Command | Exit | Detail |
|---|---|---|---|
| Invariant check | `integration_gate.py` Step 3 | 0 | **PASS** — 8 firm invariants via `validate_architecture.py` Check D (INV-008 now machine-checked via `def close_slice` grep) |
| Ruff | `integration_gate.py` Step 4a | 1 | **FAIL** — 3 errors: 2× E741 `l` in `tests/unit/test_cross_slice_isolation.py:72-73` (NEW, Slice-3 slip), 1× F841 unused `result` in `tests/unit/test_post_timeout_reconcile.py:101` (inherited, on handoff list) |
| pytest (full) | `uv run pytest tests/ --tb=no -q` | 1 | **FAIL** — 11 failed, 649 passed, 3 skipped. All 11 match handoff's "inherited reds" categories (see §2) |
| integration_gate overall | `integration_gate.py` | 1 | FAIL-fast at Step 4a; failures are all known/classified |
| snapshot_diff | `snapshot_diff.py --diff` | 1 → 0 | 33 paths out-of-envelope (classified §4); `--snapshot` applied; re-run exits 0 |
| D1 bypass log rolling window | `.claude/d1-bypasses.log` | — | Missing/empty (ADR_D1_BYPASS not triggered in 2-slice window); no design-review trigger armed |

Overall: **FAIL** (integration gate non-green; all failures pre-classified — no novel regressions).

---

## Findings

### 1. INV-004 session-start budget RED — **harness drift (CC 2.1.114 → 2.1.116), not cairn content**

`test_inv004_turn1_token_budget` fails at 30170-30351 tokens vs. 30000 hard budget. Critically, the regression source is a Claude Code harness minor-version bump, not cairn content growth:

| CC version | Turn-1 tokens | Budget status | Source |
|---|---|---|---|
| 2.1.114 | 29768 | PASS (232 below ceiling) | Sweep #21 measurements snapshot |
| 2.1.116 | 30351 | FAIL (+351 over ceiling) | This sweep's run (`docs/plans/measurements/2026-04-12-slice-003.txt` diff) |

Delta: **+583 tokens attributable to the CC 2.1.114 → 2.1.116 bump** — system-prompt overhead outside cairn's control. Direct parallel to the prior `housekeeping/inv004-rebaseline` slice (2026-04-16) which re-baselined for CC 2.1.110's ~8k-token addition per ARCHITECTURE.md INV-004 body.

D1 baseline (27314) and cairn-owned session-start content are unchanged across the window.

**Class:** `environmental` (harness-version drift).
**Action:** `housekeeping/post-inv008-tech-debt` should include **an INV-004 re-baseline for CC 2.1.116** (pattern: prior `housekeeping/inv004-rebaseline` slice). Trimming cairn content is NOT the right fix — it would erode real headroom to mask environmental drift.

### 2. 11 pytest failures + 1 F841 match handoff's inherited-reds list — **real, inherited, queued**

Full failure list (category → test):
- INV-004 budget → `test_context_budget::test_inv004_turn1_token_budget`
- learning.md cap → `test_context_discipline_protocol::test_v5_learning_staging_ground_exists_and_is_minimal`
- relpath bypass (3) → `test_hook_relpath_bypass::{test_edit_body_bare_relative_flat_slug_blocked, test_edit_body_bare_relative_legacy_blocked, test_edit_body_slice_system_flat_slug_blocked}`
- hook tolerance (3) → `test_hook_tolerance::TestV3FrontmatterEditBothForms::{test_edit_body_flat_slug_blocked, test_edit_body_flat_slug_emits_message, test_edit_body_legacy_blocked}`
- handoff-tracked → `test_housekeeping_post_slice_a_tidy::test_item_b_handoff_not_tracked`
- INV-008-aware count assertions (2) → `test_invariant_assertions::TestSlice011AssertionCoverage::{test_no_extra_assertion_blocks, test_invariant_count_unchanged}`

Plus 1 ruff F841 → `test_post_timeout_reconcile.py:101` (unused `result`).

All 12 items map 1:1 onto the handoff's "11 inherited reds" list (the F841 is tracked as a 12th line item; count matches when reds are counted by test cases). Already queued for `housekeeping/post-inv008-tech-debt`.

**Class:** `pre-existing` across all 12.
**Action:** no new slice needed; already queued. Confirm the housekeeping slice addresses all 12 when it lands.

### 3. 2 NEW ruff E741 errors in Slice-3 Phase-2 RED suite — **real, Slice-3 audit gap**

`tests/unit/test_cross_slice_isolation.py:72-73` uses `l` (ambiguous) in two list comprehensions. This file landed at `95c1beb` (Slice-3 Phase-2 RED suite). Ruff was not run (or errors were not observed) during Phase-2 or the manual Phase-4 recovery — the handoff explicitly notes "the orchestrator did NOT drive Phase 4 — it escalated mid-Phase-3 after the re-dispatch cap (`run_phase_loop` B15) fired on a triager misroute."

**Class:** `slice-caused (C)` — introduced by `compression/slice-3-observability-and-close-slice` but not caught because Phase 4 ran manually and skipped the ruff gate.

**Process lesson:** manual Phase-4 recovery runs should checklist integration_gate Steps 4a (ruff) + 4b (pytest) before committing the close handoff. Candidate addition to `docs/lessons.md` or a tightening of `handoff.full.md` Phase-4 protocol.

**Fix:** trivial — rename `l` to `line` in both comprehensions. Two options:
- **(a)** Roll into `housekeeping/post-inv008-tech-debt` (low overhead; consistent with the inherited-reds scope).
- **(b)** Fix immediately as a pipeline-substrate commit. Not recommended — per rule 9, failures route through new slices.

Recommended: option (a).

### 4. Snapshot drift: 33 out-of-envelope paths across 2-slice window — **expected, classified**

All 33 paths fall into three buckets:

| Bucket | Count | Paths (abbreviated) | Class |
|---|---|---|---|
| Slice-3 core work | 23 | `docs/adr/{slice-close-contract,orchestrator-observability}.md`, `docs/plans/2026-04-20-*.md` (3), `scripts/slice_orchestrator.py`, `tests/integration/test_signal_observability.py`, `tests/integration/test_compressed_slice_end_to_end.py`, 17 new `tests/unit/test_*.py` (observability_writer, close_slice_hardened, resume_reconcile, heartbeat, signal_handler, state_schema, etc.) | `slice-caused (C)` — expected Slice-3 envelope |
| Pipeline-substrate pre-Slice-3 | 4 | `docs/ARCHITECTURE.md` (refresh at `ff6a08e`), `docs/adr/index.md` (ADR landings), `docs/lessons.md` (L-007 at `9bc65f9`), `tests/unit/test_agent_prompt_updates.py` (new Slice-3 RED) | `pipeline-substrate` — /decision + /refresh-architecture output |
| Slice-2 carryover | 6 | `checks/role_guard.py` (JSON envelope at `779e7f5`), and Slice-2 orchestrator state-machine tests updated in-place | `slice-caused (C)` — expected Slice-2 envelope, not swept at Slice-2 close |

Baseline refreshed at tip `c050772`. Re-run of `snapshot_diff.py --diff` exits 0.

### 5. `CAIRN_HEARTBEAT_INTERVAL` / `CAIRN_HEARTBEAT_STALE` not yet documented — **real, handoff item still valid**

Grep of `docs/operational-reference.md` for either env var returns no matches. Handoff queues this at `operational-reference.md:339-344` neighborhood. Matches the CLAUDE.md rule: "No hardcoded timeouts/sizes in consumer-facing scripts... document the var in `docs/operational-reference.md`." These vars are implemented (`heartbeat.py`, `slice_orchestrator.py`) but undocumented.

**Class:** `pre-existing` documentation debt.
**Action:** handoff item stands; can be folded into `housekeeping/post-inv008-tech-debt` or handled as a standalone 1-commit doc patch.

### 6. Provisional `orchestrator-observability` referenced by firm INV-008 — **observation, not a blocker**

INV-008 is firm. Its attribution line reads `(slice-close-contract; orchestrator-observability)`. The prose mentions heartbeat advisory and cross-slice `.claude/orchestrator-debug/` placement — both orchestrator-observability content.

**Check:** the machine-checked assertion for INV-008 only greps `def close_slice` in `scripts/slice_orchestrator.py`. It does not assert heartbeat or schema v1.0. So the firm-check-surface is purely slice-close-contract (firm). The orchestrator-observability material is descriptive prose inside a firm invariant, not elevated to firm-enforcement.

**Class:** governance observation. Acceptable under INV-004 D2 (assertions can be narrower than prose), but worth surfacing if orchestrator-observability supersedes in the future — the INV-008 prose will need a regeneration pass.

---

## Staleness summary (handoff Blocked/Pending audit)

| Handoff item | Status | Action |
|---|---|---|
| 11 inherited reds → `housekeeping/post-inv008-tech-debt` | **Still valid** | Confirmed by this sweep — all 11 still RED; plus the F841 on `test_post_timeout_reconcile.py:101` = 12 items total for that slice |
| Document `CAIRN_HEARTBEAT_INTERVAL` + `CAIRN_HEARTBEAT_STALE` at `operational-reference.md:339-344` | **Still valid** | Confirmed — neither var documented (§5) |
| Triager misroute on superseded tests → prompt iteration | **Still valid** | Prompt iteration still pending; memory `triager_misroute_on_superseded_tests.md` intact |
| Path C + multi-instance hardening → future slices | **Still valid, deferred** | No action this sweep |
| Ruff F841 at `tests/unit/test_post_timeout_reconcile.py:101` | **Still valid** | Confirmed RED (§2) — roll into `housekeeping/post-inv008-tech-debt` |
| (New) 2× E741 in `tests/unit/test_cross_slice_isolation.py:72-73` | **Add to handoff** | Slice-3 audit gap (§3); add to `housekeeping/post-inv008-tech-debt` scope |

No handoff items have been silently resolved by the 2-slice window; all remain valid post-sweep.

---

## Follow-up slice candidates (for operator triage)

1. **`housekeeping/post-inv008-tech-debt`** — previously queued; scope now confirmed as **12 items + 1 re-baseline**:
   - 11 pytest reds (§2 list), with INV-004 specifically requiring a **CC 2.1.116 re-baseline** (§1), not cairn-content trimming
   - 2 ruff E741 + 1 ruff F841 (merge §2 F841 + §3 E741)
   - Optional: `CAIRN_HEARTBEAT_*` doc patch (§5) — could go here or standalone
   Priority: medium (INV-004 is harness-drift-caused, not load-bearing for slice pipeline correctness).

2. **Triager prompt iteration (no slice id yet)** — before the next firm-contract slice, tighten the triager's RAISE_ISSUE routing so superseded-test names don't default to RE_DISPATCH-to-Phase-2. Memory `triager_misroute_on_superseded_tests.md` has the context.

3. **Optional lesson addition** — `docs/lessons.md` entry: "Manual Phase-4 recovery must re-run `integration_gate` Steps 4a+4b before committing close handoff; otherwise Phase-2 RED-suite lint slip through (evidence: Slice-3 E741 leak §3)."

No new firm ADRs required. No novel cross-slice regressions. All findings resolvable via the already-queued housekeeping slice.

---

## Next action

`sweep.yaml` → `last-sweep-at-slice-id: compression/slice-3-observability-and-close-slice`. Commit this sweep report + yaml bump together.
