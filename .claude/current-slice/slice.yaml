id: compression/phase-1-handoff-stage-surface
name: compression/phase-1-handoff-stage-surface
status: complete
current_phase: 4
brief: "Extend scripts/slice_orchestrator.py:commit_phase_handoff (lines 1441-1452)\
  \ to stage phase-1-writer full declared write surface \u2014 BOTH .claude/current-slice/intent.md\
  \ AND .claude/features/<feature>.yaml \u2014 alongside the existing handoff-phase-<N>.md\
  \ stage. Per .claude/agents/phase-1-writer.md:9 the declared writes are both files;\
  \ today commit_phase_handoff stages only handoff-phase-N.md, leaving BOTH orphaned.\
  \ Evidence of the chronic intent.md orphan: 8e619c3 slice(amendment), 513b0de phase-1-artifact-recovery,\
  \ a3729cb phase-1-artifacts (manual persistence), f2ab0f3 phase-1-intent (seeded).\
  \ Evidence of the features-file orphan: a8d8f23 and dbea33c chore(orchestrator)\
  \ tail-commits. One function, one fix: after the existing if path.exists(): _git(\"\
  add\", str(path)) for handoff-phase-N.md at line 1450, add best-effort stages for\
  \ both Phase-1 paths. Feature-id resolves from slice.yaml id (segment before /)\
  \ via the existing _slice_id() helper. Each stage guarded by if path.exists(): _git(\"\
  add\", str(path)) mirroring the existing pattern so Phases 2/3/4 (which do not touch\
  \ either path) remain no-ops. No git add -A. No file creation. No mkdir. Phase-2\
  \ writes FOUR RED tests: R1a intent.md stages when present, R1b features-file stages\
  \ when present, R2a no-op when intent.md absent, R2b no-op when features-file absent.\
  \ Preserves INV-008 DC-4 (close_slice remains sole slice-complete commit source;\
  \ commit_phase_handoff stays a per-phase boundary staging function, not a new terminal\
  \ commit site). Preserves INV-003 (phase-1-writer contract unchanged; only orchestrator\
  \ staging surface extended). Out of scope: Slice C F2/candidate-set hygiene (pending\
  \ /decision on shape a vs b); close_slice edits (Slice E landed those; symmetrical\
  \ but separate function); init_new_slice edits (distinct commit site \u2014 init_new_slice\
  \ owns slice.yaml only; Phase-1 outputs land at the Phase-1 handoff boundary, not\
  \ init); phase-1-writer agent-contract changes (.md:9 declared surface is authoritative);\
  \ feature-file or intent schema edits; generalised git add -A rewrites; Slice D\
  \ envelope-immutability-guard; Slice F rolling-window /status surfacing; Path C\
  \ fleet-writes empirical gap; any new commit sites. Mirror Slice E edit discipline\
  \ \u2014 additive stage-list extension, not a rewrite."
